# PMS 숙박 예약 시스템

## 이 프로젝트가 무엇인가

숙박 예약 시스템을 만드는 과제 프로젝트다. 마감은 2026-08-17 23:59:59.

과제의 본질은 CRUD를 만드는 게 아니다. **같은 객실을 여러 사람이 동시에 예약할 때 무슨 일이 벌어지는가**를 제대로 다루는 것이다. 동시성 제어, 멱등성, 상태 전이가 이 프로젝트의 심장이다. 기능을 넓히는 것보다 이 셋을 깊게 파는 쪽이 항상 우선한다.

## 작업 전에 읽어야 하는 문서

작업 종류에 따라 아래 문서를 **반드시 먼저 읽는다.**

| 작업 | 읽을 문서 |
|---|---|
| 코드를 쓰기 전 (모든 경우) | `docs/architecture/coding-rules.md` |
| 패키지·레이어 배치를 결정할 때 | `docs/architecture/clean-architecture.md` |
| 도메인 모델을 만들거나 고칠 때 | `docs/architecture/ddd.md` |
| 테스트를 쓸 때 | `docs/architecture/tdd.md` |
| 새 feature를 시작할 때 | `docs/spec/TEMPLATE.md`, 그리고 해당 feature 스펙 |
| 문서에 시퀀스·도메인·ERD·상태머신을 그릴 때 | `docs/architecture/diagram-rules.md` |
| 여러 세션이 병렬로 돌 때 | `docs/architecture/parallel-work.md` |

## 기술 스택

확정된 것이며, 바꾸려면 ADR을 남긴다.

| 항목 | 선택 | 비고 |
|---|---|---|
| 언어 | Python | |
| 프레임워크 | FastAPI | 동기(sync) 기준. async 채택 여부는 F01 스펙에서 결정 |
| ORM | SQLModel (SQLAlchemy) | |
| 조립 | Dependency Injector | 포트·어댑터를 컨테이너로 조립 |
| DB | MySQL 8.4 | 일별 재고 테이블 모델링 |
| 스키마 | Alembic | 스키마의 진실은 SQL. 자동 생성에 기대지 않는다 |
| 캐시·락 | Redis 7.4 | 멱등성 키, 분산락, 조회 캐시 |
| 테스트 | pytest + Testcontainers | SQLite 사용 금지 |
| 부하테스트 | k6 | |
| 인증 | `X-User-Id` 헤더 | 이 과제의 관심사가 아님을 문서에 명시 |

2026-08-15에 Java 21 / Spring Boot 4에서 위 스택으로 전환했다. 근거는 [ADR-0050](docs/decisions/ADR-0050-기술-스택-전환.md)에 있다. 전환 시점에 구현 코드가 0줄이었고, 남은 시간에 구현과 검토를 모두 하려면 익숙한 도구가 필요하다는 판단이었다.

## 핵심 설계 결정

읽지 않고 코드를 쓰면 리뷰에서 반려된다.

**도메인 모델과 ORM 모델은 통합한다.** 도메인 객체를 `SQLModel` 테이블 클래스로 직접 만든다. 대신 도메인은 `sqlmodel`·`sqlalchemy` 외의 프레임워크에 의존하지 않는다. FastAPI, Redis, Dependency Injector 관련 타입이 `domain` 모듈에 들어오면 안 된다.

**상태 전이는 전이 테이블로 구현한다.** `(현재 상태, 이벤트) -> 다음 상태` 맵을 두고 그 표로만 판단한다. if-else 분기로 상태를 전이시키지 않는다. `setStatus` 같은 세터는 만들지 않는다. 외부는 이벤트만 던진다.

**재고는 날짜 단위 행으로 관리한다.** 기간을 통째로 다루지 않고 `(객실타입, 날짜)` 단위 행에 잔여 수량을 둔다. 이 행이 동시성 제어의 대상이다.

## 워크플로우

하나의 feature는 아래 순서로 진행한다. 각 단계는 대응하는 스킬이 있다.

```
스펙 작성          → .claude/skills/feature-spec
  ↓ [관리자 승인]   → .claude/skills/approval-report
개발 사이클        → .claude/skills/dev-cycle
  TDD → 구현 → 테스트 → 리뷰어 3명 → 피드백 반영 → 반복
  ↓ [관리자 승인]   → .claude/skills/approval-report
완료
```

결정이 하나 정해질 때마다 `.claude/skills/decision-log`로 ADR을 남긴다. 새 개념이 나오면 `.claude/skills/learning-note`로 제목만 먼저 쌓는다.

**승인 게이트를 건너뛰지 않는다.** 보고서를 쓰고 관리자가 승인해야 다음 단계로 간다.

**세션 역할.**

- **PM 세션 (하나):** 하네스·규칙 문서 유지보수, 전체 범위와 feature 분할(`docs/spec/00-problem-and-scope.md` 소유), feature 세션 착수 브리핑 작성, 공유 파일 중재와 병합 순서 결정, PR 병합. **구현 코드와 feature별 스펙은 작성하지 않는다.**

  **PM은 스펙 층에서 멈춘다.** 무엇을 만드는가, 어떤 유스케이스가 범위 안인가, 어느 feature가 무엇을 소유하는가까지가 PM의 층이다. **어떻게 만드는가는 아니다.** 함수 이름, 테이블 컬럼, 훅 시그니처, 테스트 설계, 락 구현은 그 코드를 소유한 세션이 정하고 세션끼리 직접 맞춘다.

  PM이 기술 질문에 답하면 두 가지가 나빠진다. 세션이 이미 아는 것을 PM이 옮기느라 왕복이 늘고, PM은 코드를 안 보므로 **틀린 답이 섞인다.** 실제로 이 프로젝트에서 PM 조언 일곱 건이 세션 반박으로 철회됐다(`docs/reports/낭비-정리.md`).

  **구현이 시작되면 PM에게 보고하지 않는다.** PM은 현황판과 git으로 상태를 본다. 진행 보고를 받아야 아는 상태라면 그 현황판이 잘못 만들어진 것이다. PM이 세션에 말을 거는 경우는 병합했을 때와 순서를 바꿔야 할 때뿐이다.
- **feature 세션 (feature당 하나):** 자기 feature의 스펙 작성 → 관리자 승인 → dev-cycle(TDD·리뷰 반복) → 완료 승인까지 전 과정을 담당한다. 다른 feature와는 parallel-work 규칙(소유 패키지, Flyway 대역)으로 격리된다.
- 모든 feature 세션은 스펙 작성부터 시작한다. (PM이 작성했던 F01 스펙 초안은 2026-08-15 폐기됨. 그 초안에 대한 검토 기록은 `docs/reviews/F01-spec-review-업계정합성.md`에 있으며 새 스펙의 입력 자료다)
- **feature 세션은 자기 착수 브리핑(`docs/briefings/<feature>.md`)을 읽는 것으로 시작한다.** 브리핑에 읽을 문서 순서, 소유 범위, 시작 단계가 있다.

feature 세션은 **이 저장소의 문서만 보고** 작업할 수 있어야 하며, 대화 맥락을 전제하는 정보에 의존하지 않는다.

**막혔을 때 어디로 가는가는 무엇이 모호한지에 달렸다.**

| 모호한 것 | 어디로 |
|---|---|
| 이 기능을 만드는 게 맞나, 이 유스케이스가 범위 안인가 | **관리자에게 직접** |
| 다른 feature의 코드가 어떻게 생겼나, 계약이 뭐라고 하나 | **그 세션에 직접 묻는다** |
| 어떻게 구현할까 | 스스로 정한다. 소유자니까 |

**한번 시작하면 알아서 진행한다.** 매 단계 승인을 받지 않고, 진행 상황을 보고하지도 않는다. 스펙과 Task 목록이 이미 무엇을 만들지 정해뒀으니 그대로 간다.

**결정이 필요한 것이 나오면 둘 중 하나를 한다.**

- **지금 답이 없으면 못 가는 것** — 관리자에게 바로 묻는다. PM을 거치지 않는다
- **일단 정하고 갈 수 있는 것** — 정하고 진행하되 **스펙의 결정 검토란에 기록한다.** 왜 그렇게 정했는지, 뒤집히면 뭘 다시 해야 하는지까지 적는다. 다음 승인 게이트에서 관리자가 한 번에 본다

**둘을 가르는 기준은 되돌리는 비용이다.** 나중에 고쳐도 코드 몇 줄이면 정하고 간다. 스키마나 계약이 바뀌어야 하면 지금 묻는다.

**범위 질문의 답은 문서에 반영한다.** 기술 질문의 답은 코드와 테스트에 남으므로 따로 적을 필요 없다.

**승인 문서에는 결정 검토란을 반드시 둔다.** 작성자가 내린 결정을 D 항목(결정·이유·트레이드오프·대안·확인 질문)으로 전부 모으고, 관리자의 동의를 항목 단위로 받는다. 결정을 본문에만 묻어두지 않는다. 검토 문서는 가능한 환경이면 `open -a Bear <파일>`로 열고 SendUserFile로도 보낸다. 두 도구가 없는 환경이면 파일 경로와 GitHub 링크를 안내하는 것으로 대체한다.

## 문서 구조

```
docs/
├── architecture/   구조·규칙 정의. 작업 시 참조하는 문서
├── briefings/      feature 세션 착수 브리핑. 세션의 시작점
├── spec/           문제 정의(00)와 feature별 스펙
├── decisions/      의사결정 이력 (ADR)
├── reviews/        스펙·코드 리뷰 기록
├── reports/        승인 게이트 보고서
├── learning/       학습 노트. 제출 대상이 아니다
├── load-test/      k6 시나리오 설계와 결과 (F04에서 생성)
└── submission/     제출용 최종 문서만
```

## 언어

사용자가 읽는 모든 문서와 대화는 한국어로 쓴다. 코드 주석도 한국어로 쓴다. 커밋 메시지도 한국어로 쓴다.

**전문 용어를 그대로 던지지 않는다.** 관리자가 검토하는 문서에서 개념을 처음 꺼낼 때는 쉬운 한국어로 풀어 쓴다. "바운디드 컨텍스트 2분할과 트랜잭션 예외 방침"이 아니라 "재고와 예약을 별도 구역으로 나누고, 예약 생성만 두 구역을 한 묶음으로 처리한다"로 쓴다. 영어 용어는 코드와 대응이 필요한 곳(클래스명, 설정 키)에만 쓴다.

에이전트끼리 주고받는 중간 산출물은 영어여도 된다.

## 절대 규칙

- `docs/architecture/coding-rules.md`를 읽지 않고 코드를 쓰지 않는다.
- 테스트 없이 구현하지 않는다. 실패하는 테스트를 먼저 쓴다.
- 동시성 관련 코드는 **반드시** 동시성 테스트를 동반한다. 단일 스레드 테스트만으로는 통과시키지 않는다.
- 승인 게이트를 임의로 건너뛰지 않는다.
- 병렬 세션에서 다른 feature가 소유한 도메인 패키지를 수정하지 않는다.
