# [F00] 승인 요청: (스펙 검토 / 개발 완료)

- 날짜:
- 단계: 스펙 승인 / 개발 완료 승인
- 판정 요청: 승인 / 조건부 승인 / 재작업

---

## 한눈에 보기

관리자가 이 절만 읽고도 판단할 수 있어야 한다. 다섯 줄을 넘기지 않는다.

- 무엇을 했는가:
- 핵심 판단:
- 확인이 필요한 것:
- 리스크:

---

## 1. 무엇을 만들었는가

기능 목록이 아니라 **문제와 해법**으로 쓴다.

## 2. 코드 구조

만든 파일을 레이어별로 보여준다. 각 파일이 무슨 책임을 지는지 한 줄씩 쓴다.

```
com.pms.reservation
├── domain/
│   └── ...
```

레이어 규칙을 어긴 곳이 있으면 여기서 먼저 밝힌다. 숨기지 않는다.

## 3. 도메인 흐름

핵심 유스케이스의 흐름을 그림이나 표로 보여준다. 도메인 객체가 어떻게 생기고 바뀌는지가 드러나야 한다.

```
요청 → 멱등성 확인 → 락 → 재고 차감 → 예약 생성 → 커밋 → 락 해제
```

상태 전이가 있으면 표를 넣는다.

## 4. 동시성과 멱등성

이 절은 반드시 채운다.

**경합 지점과 방어 수단**

| 경합 지점 | 방어 | 검증 방법 |
|---|---|---|

**1차 방어선을 껐을 때 결과**

분산락을 끄고 돌린 동시성 테스트 결과를 쓴다. 불변식이 지켜졌는가.

## 5. 테스트 결과

실제 실행 결과를 붙인다. 요약만 쓰지 않는다.

```
BUILD SUCCESSFUL
Tests: 00 passed, 0 failed
```

| 계층 | 개수 | 결과 |
|---|---|---|

**동시성 테스트 상세**

| 시나리오 | 스레드 | 기대 | 실제 |
|---|---|---|---|

## 6. 리뷰 이력

| 라운드 | 심각 | 중요 | 결과 |
|---|---|---|---|

반박한 지적이 있으면 그 근거를 여기에도 요약한다.

## 7. 남은 것

- 이번에 하지 않은 것과 이유
- 다음 feature로 넘긴 것

## 8. 결정 검토란

이 작업 구간에서 **작성자가 내린 결정**을 전부 모은다. 결정마다 이유와 트레이드오프를 쓰고 확인 질문을 붙인다.

### D1. 결정 제목

- **결정:**
- **이유:**
- **트레이드오프:** 포기한 것. "없음"은 허용하지 않는다
- **대안이었던 것:**
- **확인 질문:**

관리자는 번호로 답한다. **모든 D 항목에 동의를 받아야 승인이다.**

## 9. 판단이 필요한 지점

작성자가 결정하지 못해 관리자의 결정이 필요한 것을 질문 형태로 쓴다. 각 질문에 **내 추천과 근거**를 붙인다. 질문만 던지지 않는다.

| 질문 | 추천 | 근거 |
|---|---|---|

---

## 승인란

- [ ] 승인
- [ ] 조건부 승인 (조건: )
- [ ] 재작업 (사유: )
