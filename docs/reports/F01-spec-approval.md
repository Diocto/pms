# [F01] 승인 요청: 스펙 검토

- 날짜: 2026-08-15 · 단계: 스펙 승인 · 판정 요청: 승인
- 대상: `docs/spec/F01-예약-코어.md` · 브랜치: `worktree-F01`
- 이 보고서는 **판단에 필요한 것만** 담았습니다. 근거·다이어그램·전체 형식의 D 항목은 전부 스펙에 있고, 절 번호로 가리켰습니다.

---

## 한눈에 보기

예약의 전 생명주기(생성·확정·취소·만료·체크인아웃)와 일별 재고 차감·복원을 설계했습니다. 폐기된 초안은 쓰지 않고 새로 썼습니다.

### 진행 상황 (2026-08-15 갱신)

| 항목 | 판단 | 반영 |
|---|---|---|
| **D1** 인원수 필드 + 정원 검증 | ✅ **채택** — "가벼운 로직으로 보이니 넣자" | 반영 완료 |
| **D4** `NO_SHOW` 상태 | ⏸️ **유예** — "추후 작업으로 남겨두자. 우선은 예약 코어부터 모두 구현" | 전이 표 49→**36칸**, 스케줄러·인덱스·API 제거. **체크인 상한은 살림.** 남는 구멍은 한계로 문서화 |
| **D7** 확인번호 | ✅ **채택(형식 지정)** — "우리만의 key로. 예약 날짜+호텔+객실+무작위. **단 포맷에 의한 로직은 넣지 말 것**" | `260901-H1R3-K7M2XQ4R` 형태로 확정. **포맷을 읽는 코드를 만들지 않는다** — T25b가 강제 |
| **D10** 날짜 단위 다중 락 | ✅ **채택** — "lock ordering 고려. **바깥에서 실수해도 안에서 정렬**하도록" | `acquireAll`이 받은 목록을 스스로 정렬. 재고 UPDATE도 같은 목록을 공유 |
| **D17** 락 On/Off | ✅ **채택(범위 조정)** — "운영이 아니라 프로젝트 환경변수, feature flag 느낌" | `PMS_LOCK_ENABLED` 환경변수 오버라이드. "운영 스위치" 명분 삭제 |
| **D22** 실제 청구 단가 기록 | ✅ **채택** — "의도적 비정규화로 보이는데 그런거라면 채택" | 정확한 이해이며 그대로 진행 |

**[필수] 5개와 D22는 전부 판단이 끝났습니다. 남은 것은 새로 추가된 D24와 [확인]·[참고] 17개입니다.**

**D24(추가)** — F04가 락 On/Off 대조를 돌리기 전에 **스위치가 실제로 꺼졌는지 확인**해야 합니다. 확인 없이 돌리면 두 회차가 같은 조건이 되어도 결과가 "차이 없음"으로 나오고, 그게 **"락이 없어도 되더라"는 정반대 결론**으로 읽힙니다. 이미 열려 있는 `/actuator/info`에 락 설정값을 싣는 것으로 정했습니다 — **새로 노출되는 것이 없고 코드는 빈 하나입니다.** → 부록 A의 D24

> ⚠️ **미결 17개 중 다섯은 다른 세션이 이미 전제로 삼고 있습니다** — D2(타임존 고정), D16(재고 자연키 PK), D18(멱등 재요청 200), D21(`checkOut > today`), D23(사전 검사 훅 위치). **"확인·참고 전부 동의" 한마디면 전부 닫힙니다.**

확인되면 곧바로 (1) 공유 파일 2건을 선행 커밋으로 `main`에 올리고, (2) TDD 개발 사이클에 들어갑니다. **F02·F03·F04 세 세션이 이것을 기다리고 있습니다.**

> **스펙 문서가 2026-08-15에 4단 구조(도메인 구조 / 유스케이스 흐름 / 기술 스택 / 프로젝트 구조)로 재정렬됐습니다.** 계약은 그대로이고 배치만 바뀌었으며, 옛 절 번호 대응표를 문서 0.2절에 뒀습니다.

---

## 1. 무엇을 만들기로 했는가

동시성 방어의 축을 **조건부 UPDATE 세 줄**로 좁혔습니다. 재고 차감, 상태 전이, 재고 복원. 읽고-판단하고-쓰는 세 단계를 SQL 한 줄로 줄이면 그 사이에 끼어들 틈이 없어집니다.

```sql
UPDATE room_daily_inventory SET remaining = remaining - :count
 WHERE room_type_id = :roomTypeId AND stay_date = :date AND remaining >= :count
```

가장 중요한 판단은 **재고 복원을 상태 전이의 "결과"로 묶은 것**입니다. 재고를 되돌리려는 요청은 셋(취소·결제 실패·만료)인데 되돌릴 기회는 한 번뿐입니다. 그 "한 번"을 상태 전이 UPDATE가 정합니다 — 상태를 바꾸는 데 성공한 하나만 갱신 행 수 1을 받고, 나머지는 0을 받아 **복원 코드에 아예 들어가지 못합니다.**

셋째 방어선은 `CHECK remaining <= total_quantity`입니다. 하한(`>= 0`)은 다들 챙기지만 **상한이 없으면 이중 복원이 아무 신호 없이 성공합니다.** 팔 수 있는 방보다 많은 방이 생기는데 아무도 모릅니다.

→ 상세: 스펙 3.2절 / 상태 전이 표(36칸): 스펙 1.4절 / DB 설계: 스펙 1.5~1.9절

## 2. 불리한 것 먼저

- **테스트를 하나도 돌리지 않았습니다.** 스펙 단계라 코드 변경이 0건입니다(`git status`상 `src/` 변경 없음). 4절의 수치는 전부 *기대값*이며 개발 완료 보고서에서 실측으로 대체합니다.
- **레이어 규칙 예외 1건.** 예약 생성이 재고·예약 두 애그리거트를 한 트랜잭션에서 고칩니다. `ddd.md` 원칙 위반이지만 **00 문서 D6에서 승인된 예외**이고, 스펙에 예외임을 명시했습니다.
- **닫지 못한 틈 1건.** 모의 결제 성공 직후 크래시하면 결제만 되고 예약은 `PENDING`으로 남아 만료됩니다. 이번에 닫지 않고 한계로 문서화했습니다(D5). 실 PG라면 결제 원장 + 정산 배치가 필요합니다.
- **확장 지점이 넷이고 구현자가 F02 하나뿐입니다.** 명백한 설계 냄새라 스펙 D23에 적었고, 다섯 번째가 필요해지면 훅을 더 만들지 않고 F02가 자기 진입점을 갖는 쪽을 재검토한다는 선을 그었습니다.

## 3. 동시성 방어 요약

| 경합 | 방어 | 증명 |
|---|---|---|
| 같은 재고 행 (초과 판매) | 조건부 UPDATE + CHECK `>= 0` | K1: 잔여 10 / 100스레드 → 성공 정확히 10 |
| 여러 재고 행 (데드락) | 날짜 오름차순 전순서 고정 | K8: 반대 순서 요청 → 데드락 0건 |
| 같은 멱등 키 | Redis `SET NX` + DB `UNIQUE` | K3: 같은 키 50 동시 → 예약 1건 |
| 확정↔취소↔만료 (이중 복원) | 복원을 전이 성공의 결과로 묶음 + CHECK `<= total_quantity` | K6·K10: 복원 1회, 이력 줄 1개 |

**1차 방어선을 끈 상태의 증명이 핵심입니다.** `pms.lock.enabled=false`로 분산락을 통째로 끄고 K1과 같은 조건으로 돌려(K2), 여전히 성공 10건·잔여 0이어야 합니다. 멱등성도 같은 방식으로 Redis를 무력화하고 DB 유니크 제약만으로 검증합니다(K4).

동시성 시나리오는 10개(K1~K10)입니다. → 스펙 4.3절

## 4. 결정 검토란

### [필수] 5개 — **판단 완료 (2026-08-15)**

다섯 항목 모두 답을 받았습니다. 판단 내용과 반영 결과는 위 「진행 상황」 표에 있고, 전체 형식(결정·이유·트레이드오프·대안)은 스펙 부록 A의 해당 D 항목에 그대로 있습니다.

| # | 결과 | 스펙 |
|---|---|---|
| D1 | ✅ 채택 — 인원수 필드와 정원 검증 | 부록 A D1 |
| D4 | ⏸️ 유예 — 노쇼는 추후 작업. 전이 표 36칸으로 축소, **체크인 상한은 살림** | 부록 A D4, 1.4절 |
| D7 | ✅ 채택(형식 지정) — 자체 포맷 확인번호, **포맷 의존 로직 금지** | 부록 A D7 |
| D10 | ✅ 채택 — 날짜 단위 다중 락, **정렬은 락 포트 안에서** | 부록 A D10, 3.3절 |
| D17 | ✅ 채택(범위 조정) — 프로젝트 feature flag 수준 | 부록 A D17 |

**D7·D10의 확인 요청에는 각각 "무엇인가"와 "어디서 어떻게 쓰는가"를 스펙 부록 A에 풀어 적었습니다.**

### [확인] 11개 — 동의 여부만 알려주십시오

> **D21·D22는 이 보고서를 제출한 뒤 추가되었고, D12는 개정되었습니다.** 병렬 세션(F02·F03)과 규칙이 갈리는 지점이 발견되어 확정한 것들입니다. 나머지 항목은 제출 시점 그대로입니다.
>
> **셋 중 D22를 먼저 봐주십시오.** 이것만 성격이 다릅니다 — 나머지는 편의나 정합성 문제인데, D22는 **되돌릴 수 없는 데이터를 틀리게 저장하느냐**의 문제입니다.


| # | 결정 | 스펙 |
|---|---|---|
| D2 | 업무 시각을 `Asia/Seoul` 고정 (`ClockConfig` 선행 커밋). 지금은 로컬 KST / 컨테이너 UTC로 9시간 어긋남 — 동의? | 부록 A |
| D3 | CHECK 제약 전부 걸기(재고 상·하한, 객실 수, 기간, 인원, 금액). VO와 중복되지만 최후 방어선 — 동의? | 1.7절 |
| D5 | 확정 중 크래시 틈을 닫지 않고 **한계로 문서화**. 실 PG면 결제 원장 필요하다는 기록만 남김 — 동의? | 2.3절 |
| D6 | `price_per_night` 단가 스냅샷 저장. 지금은 `base_price`와 값이 같지만 **사후 백필 불가** — 동의? | 1.6절 |
| D8 | 문서화 후보 4개(요금제·다타입·세금통화·확정후변경)를 **00 문서 제외 표에 추가할 것을 제안**. F01은 직접 안 고침 — PM에 전달해도 됩니까? | 부록 A |
| D15 | 취소 수수료·마감 없음(체크인 전 전액 취소), 노쇼 자동 처리 없음(D4 유예로 자동 복원 경로 자체가 없음) — 동의? | 부록 A |
| D16 | 재고 테이블만 **자연키 복합 PK** `(room_type_id, stay_date)`. 뜨거운 경로에서 잠기는 인덱스 레코드가 절반 — 동의? | 1.6절 |
| D18 | 멱등 재요청에 **200 + 저장된 결과**(최초만 201). 409는 처리 중일 때만. 409를 주면 재시도가 재시도를 부름 — 동의? | 3.4절 |
| D19 | 상태 이력 테이블 추가, **성공한 전이만** 기록. 금지 전이 증명은 36칸 전수 테스트에 맡김 — 동의? | 부록 A |
| **D22** | **(추가)** 특가 예약의 `price_per_night`에 **실제 청구 단가**를 기록. 값은 `DiscountResolver` 포트로 F02가 주고, 계산 권한은 F01이 유지. 정가를 박으면 **D6이 스냅샷을 저장한 이유가 무너지고 사후 백필도 불가능**합니다. 대가는 확장 지점이 둘로 느는 것 — 동의? (시간이 부족하면 더 싼 대안으로 후퇴 가능) | **D22**, 3.6절 |
| **D21** | **(추가)** 예약 생성 조건을 `checkIn >= today`가 아닌 **`checkOut > today`**로. 끝난 숙박은 막고 **진행 중인 투숙은 허용**(체크인이 그 구간의 사건). 원안은 검증 없음이었음. **이 규칙만 DB 최후 방어선이 없다**(MySQL CHECK에 `CURDATE()` 불가) — 동의? | **D21**, 1.9절 |

### [참고] 7개 — 이견 없으시면 넘어가셔도 됩니다

D13은 관리자 판단이라기보다 **다른 세션과 맞춰야 하는 사실**입니다. 시드의 날짜·id·수량을 스펙 **5.5절 「시드 데이터 계약」**에 숫자로 박았고, F02·F03·F04가 그 절만 보고 맞춥니다. 통합 지뢰를 하나 막은 것이라 배경만 짧게 적습니다 — 원안은 `CURDATE()` 기준 90일이었는데, 그러면 세션마다 다른 날짜를 가정하고 어긋났을 때 **응답이 "재고 부족"이라 매진과 구분되지 않아 조용히 실패합니다.**


| # | 결정 | 스펙 |
|---|---|---|
| D9 | 멱등 키를 별도 테이블 아닌 `reservation` 컬럼 + UK로 | 부록 A |
| D11 | 미결제 보류 10분, 만료 스케줄러 30초 주기 (설정값) | 부록 A |
| D12 | **(개정)** 주문서 형태 Command + 가격 계산 도메인 서비스 분리. 원래 "쿠폰 필드는 안 만듦"이었으나 **F02라는 실제 사용자가 생겨 `discounts` 자리를 지금 만드는 것으로 뒤집었습니다.** 죽은 필드가 아니게 됐습니다 — 동의? | 11절, 7.5절 |
| D13 | 시드 재고 날짜를 `CURDATE()`가 아닌 **고정 날짜** `2026-08-01`~`2026-10-29`로 박음. 호텔 2·타입 5 + **경합 실험용 소량 재고(10) 타입 1종**. 마감 이후 쓸모없어지는 것을 감수 — 동의? | **5.5절**, 11절 |
| D14 | 낙관락 미사용. 조건부 UPDATE로 일원화 (경합이 잦은 전제라 재시도 폭풍 회피) | 부록 A |
| D20 | F02를 위한 확장 훅 2개(생성·반납)를 **F01이 정의하고 F02가 구현**. F01은 promotion을 참조하지 않음. 구현이 아직 없는 인터페이스를 미리 만드는 것 — 동의? | **7.5절**, 11절 |
| **D23** | **(추가)** 락보다 **앞에서** 요청을 거를 사전 검사 훅 추가. 없으면 매진 요청이 락·트랜잭션을 다 열고 롤백하고, 특가만 2층 방어가 됨. **확장 지점이 넷이 되고 구현자는 전부 F02 하나 — 설계 냄새라고 스펙에 적었습니다** — 동의? | **D23**, 7.5절 |

## 5. 판단이 필요한 지점

스펙에 **열린 질문은 없습니다.** 다만 등급 판단에 도움이 될 두 가지를 붙입니다.

| 질문 | 추천 | 근거 |
|---|---|---|
| [필수] 5개를 다 받으면 오늘 안에 개발이 끝나는가? | **D4를 자를 여지를 남겨두시길** | D4가 유일한 순수 추가 작업입니다. 00 D4(절단 순서)가 UC-6 체크인아웃을 두 번째 절단 대상으로 뒀는데 `NO_SHOW`는 그 위에 얹힙니다. **밀리면 여기부터 자르는 것이 절단 순서와 일관됩니다.** 자르면 `CONFIRMED`에서 못 나가는 예약이 남습니다 |
| D17을 운영에서 켜는 게 위험하지 않은가? | **위험하지 않다고 봅니다** | 락을 꺼도 안전하다는 것이 우리 설계의 주장이고 K2가 그걸 증명합니다. 주장이 참이면 운영에서 꺼지는 것은 사고가 아니라 성능 저하입니다. 거짓이면 프로파일로 감출 게 아니라 설계를 고쳐야 합니다 |

## 6. 승인 직후 할 일

**승인 전에는 `main`을 건드리지 않습니다.** 승인되면 아래 두 변경만 담은 작은 선행 커밋을 먼저 올립니다(PM이 절차 확인 완료).

| 파일 | 변경 | 걸린 D |
|---|---|---|
| `common/config/ClockConfig` | `Clock`을 `Asia/Seoul` 고정 | D2 |
| `application.yml` | `pms.` 네임스페이스 키 **6개** 추가 (보류시간·만료주기·락 3종·**모의 결제 거절률**) | D10·D11·D17, UC-3 |

이어서 dev-cycle(TDD → 구현 → 리뷰어 3명 → 반영)에 들어갑니다.

---

## 승인란

- [ ] 승인
- [ ] 조건부 승인 (조건: )
- [ ] 재작업 (사유: )

**항목 번호로 답해주십시오.** 예: "필수 5개 동의, 단 D4는 일정 밀리면 자르는 조건. 확인·참고 전부 동의."
