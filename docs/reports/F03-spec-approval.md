# [F03] 승인 요청: 스펙 검토

- 날짜: 2026-08-15
- 단계: 스펙 승인
- 판정 요청: 승인 / 조건부 승인 / 재작업
- 대상: `docs/spec/F03-가용-객실-검색.md` (본문), `docs/briefings/F03.md`, `docs/spec/F03-검색-API-계약.md`

---

## 한눈에 보기

가용 객실 검색(UC-1) 스펙입니다. **검색은 정확성을 책임지지 않고 후보만 제시합니다. 재고의 진실은 예약 시점의 조건부 UPDATE(F01)가 정합니다.** 그래서 검색이 낡아 예약이 409로 실패하는 것을 버그가 아니라 정상 흐름으로 설계했습니다.

- **판단하실 것: [필수] 6개** (D2·D3·D5·D6·D9·**D15**). 나머지 10개는 4절 한 줄 표에 있습니다.
- **가장 불리한 사실:** 재고 변경을 감지할 수단이 없어 **최대 10초간 옛 결과가 보이는 것이 사고가 아니라 기본 동작**입니다.
- **승인되면 시작되는 것은 없습니다.** 구현은 F01 병합 이후이고, 그때 TDD 21단계(스펙 9절)의 1번 빨간 테스트부터 씁니다.
- 마이그레이션 변경 0건. 배정받은 Alembic 리비전 대역 `301~399`를 **한 개도 쓰지 않습니다.** 다만 **공유 파일 두 곳에 자기 줄을 추가합니다**(아래).

**2026-08-15 스택 전환(FastAPI + SQLModel + Dependency Injector)을 반영해 다시 썼습니다.** 절 단위로 훑어 유지·재작성을 분류한 결과는 `docs/reports/F03-스택전환-영향-분류.md`에 있습니다. 요점은 이렇습니다.

- **설계 판단은 하나도 뒤집히지 않았습니다.** 기존 D1~D14의 결정 내용이 전부 그대로이고, 동시성·정합성을 다루는 7절은 한 글자도 바뀌지 않았습니다
- **표현이 바뀐 것이 하나 있습니다 — D12.** "트랜잭션을 열지 않는다"는 문장이 SQLAlchemy에서는 성립하지 않아 "**세션 수명을 쿼리 한 발로 좁힌다**"로 다시 썼습니다. 의도는 같습니다
- **새 규칙이 만든 결정이 둘 늘었습니다 — D15·D16.** D15는 캐시 on/off 값을 밖에서 읽게 노출하는 것이고, **판단이 필요한 항목입니다**

### 2026-08-15 2차 갱신 — `main`이 Python 스택으로 병합된 뒤

`main` 병합(`1bb47f9`)으로 실행 환경과 규칙 개정이 들어와 스펙을 다시 맞췄습니다. **여기서도 설계 판단은 하나도 뒤집히지 않았고, 7절은 여전히 그대로입니다.** 다만 **판단하실 내용에 영향을 주는 정정이 셋** 있습니다.

| # | 무엇이 바뀌었나 | 승인에 미치는 영향 |
|---|---|---|
| 1 | **"수정이 필요한 공유 파일: 없음"이 사실이 아니었습니다** | `app/common/config.py`에 캐시 설정 2줄, `app/containers.py`에 배선 1줄이 필요합니다. 병합된 `Settings`에 락·예약·결제 설정은 있는데 **검색 캐시 설정이 없어서** 확인 중에 드러났습니다. 둘 다 `parallel-work.md`가 "절차를 밟으면 고칠 수 있다"로 허용한 파일이고 자기 줄만 추가하는 형태입니다. **전 회차에 "없음"으로 승인받으려 했으므로 여기서 정정합니다** |
| 2 | **A8의 착수 차단 조건이 해소됐습니다** | "`app/common/clock.py`가 오지 않으면 F03은 멈춘다"고 적었는데 그 파일이 실제로 병합됐습니다. `Clock`·`SystemClock`·`FixedClock`과 `KST` 고정까지 있습니다. **지금 착수를 막는 조건은 F01 병합 하나뿐입니다** |
| 3 | **D15의 노출 방식이 공용 계약으로 확정됐습니다** | 자체 엔드포인트가 아니라 `app/common/runtime_report.py`의 `RuntimeContributor` 기여자로 노출하고, F01 소유 엔드포인트가 모아 내보냅니다. 설정 키는 규칙이 정한 예외대로 환경변수 이름 그대로(`PMS_SEARCH_CACHE_ENABLED`)입니다. **D15의 확인 질문이 넷으로 늘었습니다** — 기여자 방식, 키 이름, 구현 클래스 노출, 그리고 누적 카운터를 싣지 않는 판단입니다 |

규칙 변경 자체는 넷이고 F03에는 이렇게 닿았습니다.

| 규칙 변경 | F03에 미친 것 |
|---|---|
| 층 개명 `adapter` → `presentation` + `infrastructure` | 모듈 트리 재작성. **D15가 갈 자리가 이걸로 정해졌습니다** |
| 세션 진입점 `read()` / `write()` 고정 | **D12가 규칙 쪽에서 확정됐습니다.** 조회 경로는 `read()`를 쓰고, `read()`가 `rollback()`을 부르는 이유가 D12의 근거를 하나 더 채워줬습니다 |
| 분산락은 `redis-py`의 `Lock` | **해당 없음.** F03은 락을 쓰지 않습니다 |
| 계층 간 데이터는 Pydantic `BaseModel` | 입출력 타입이 `frozen` `BaseModel`이 됐고, **D16(`~Query` 이름)이 더 자연스러워졌습니다** — 타입이 값이라 말하고 이름이 조회라 말합니다 |

**규칙 밖에서 실물을 확인하다 하나 더 나왔습니다.** 공통 `NotFoundError`의 기본 `code`가 `NOT_FOUND`인데 F04와 맺은 계약은 `RESOURCE_NOT_FOUND`입니다. 그대로 던지면 **404는 정상으로 나가고 문자열만 달라집니다.** `emptyReason` 대소문자와 같은 종류의 조용한 어긋남입니다.

**처음에는 F03에 하위 예외를 만들어 비켜가려 했는데, 고칠 자리가 `common`이라는 판정을 받아 그렇게 바꿨습니다.** `NOT_FOUND`는 어느 feature의 계약에도 없는 문자열이라 **한 번도 유효할 수 없는 기본값**입니다. F03만 우회하면 다음 사람이 같은 함정을 다시 밟습니다. F01이 기본값을 고치고, F03은 공통 예외를 그대로 던지며, **그 수정이 실제로 들어왔는지는 TDD 20번이 응답 문자열로 확인합니다.** 계약표에 A10으로 올렸고, **수정이 PR #9로 `main`에 병합되어 A10은 해소 상태입니다** — `common`의 계약 대조 테스트(`tests/test_error_contract.py`)가 상시로 지킵니다.

---

## 1. 불리한 것 먼저

| # | 불리한 사실 | 감수하는 근거 |
|---|---|---|
| 1 | **재고 변경을 감지할 수단이 없다.** 최대 10초 옛 결과가 보이는 것이 기본 동작 | F01 코드에 훅을 넣을 수 없다. 이벤트를 붙여도 **정합성 보장 수준은 그대로**고 낡음의 평균 지속 시간만 준다 (D6) |
| 2 | **검색 정확성을 스스로 보장하지 않는다.** `fresh=true`도 예약 성공을 약속하지 않는다 | 검색이 락을 걸면 조회 트래픽이 재고 차감을 느리게 만든다. 걸어도 응답 직후 재고는 또 바뀐다 (D3) |
| 3 | **Alembic 대역을 한 개도 안 쓴다.** 인덱스 튜닝 산출물이 없다 | F01의 자연키 PK가 이미 커버한다. 중복 인덱스는 재고 UPDATE 비용만 얹는다. **옳은 결론이지만 보여줄 게 준 건 사실** (D8) |
| 4 | **캐시 스탬피드 방어가 없다** | 절단 1순위 캐시 위에 방어를 얹는 건 순서가 틀렸다. F04 L5로 먼저 잰다 (D10) |
| 5 | **`salesOpenUntil`이 판매 정책이 아니라 시드에서 추론한 값** | 00 D1로 관리자 기능을 빼 "판매 기간" 개념이 도메인에 없다 (D9) |
| 6 | **검색과 예약의 날짜 경계가 다르다.** 클라이언트가 "검색에 안 보이는데 예약은 되는" 날짜를 만날 수 있다 | 검색이 과거를 허용하면 `SOLD_OUT`으로 떨어져 "매진"이라는 틀린 안내를 한다. F01이 예약에 `checkOut > today`를 스펙에 반영했고(F01 D21, **관리자 판단 대기**) 채택되면 두 규칙이 층으로 정리된다 — 예약은 진행 중인 투숙까지, 검색은 아직 시작하지 않은 투숙만 (D14) |
| 7 | **테스트 결과가 한 줄도 없다** | 스펙 단계이고 착수가 F01 병합에 묶여 있다. 계획만 제출한다 |

---

## 2. 핵심 판단 하나

| 계층 | 수단 | 막는 것 |
|---|---|---|
| 1차 | Redis 결과 캐시 | DB 부하 — **정확성 영향 없음** |
| 2차 | 단일 집계 쿼리 (락 없음) | 조회 지연 — **정확성 영향 없음** |
| 3차 | 예약 시점 조건부 UPDATE + CHECK (**F01**) | **초과 판매** |

**정확성을 지키는 계층은 3차 하나뿐입니다.** 그래서 캐시가 낡아도, 꺼도, Redis가 죽어도 초과 판매가 없습니다. 반대로 검색을 아무리 정확하게 만들어도 3차를 대신할 수 없습니다 — 검색이 재고를 홀드하지 않기로 한 이유입니다. 409를 받은 클라이언트는 `fresh=true`로 재검색하고 최대 2회 재시도합니다(스펙 6절 시퀀스 2개).

**소유 범위 준수:** `app/inventory/domain` 수정 0건(SQL로 읽기만), 재고 쓰기 0건, 마이그레이션 0개, PM 소유 문서 수정 0건. **공유 파일은 0건이 아닙니다** — `app/common/config.py`에 2줄, `app/containers.py`에 1줄을 추가합니다(2차 갱신 표 1번). 둘 다 절차가 허용한 파일이고 자기 줄만 추가하며, F01의 `errors.py` 커밋 뒤에 올립니다. 규칙과 형식적으로 어긋나는 두 곳(입력 타입을 `~Query`로 부르는 것, 세션 수명을 쿼리 구간으로만 여는 것)은 D16·D12로 올렸습니다. **`domain/` 디렉터리가 없는 것은 새 `clean-architecture.md`가 "조회 전용 경로는 도메인을 거치지 않아도 된다"를 규칙으로 명시해 더 이상 예외가 아닙니다.**

---

## 3. [필수] 판단이 필요한 6개

### D2. 도메인 애그리거트를 만들지 않고 읽기 모델만 만든다

- **결정:** `inventory.query`에 `domain` 패키지를 두지 않는다. 네이티브 집계 쿼리 + 전용 projection으로 읽고 F01 엔티티를 import하지 않는다.
- **이유:** 애그리거트는 함께 지킬 불변식의 범위인데 조회 전용에는 지킬 상태가 없다. F01 엔티티에 결합하면 F01 필드 변경이 F03 컴파일을 깬다.
- **트레이드오프:** SQL이 문자열로 노출돼 컬럼 변경이 컴파일에 안 잡힌다. `StayRange`가 F01 `StayPeriod`와 **의도적으로 중복**된다.
- **대안:** JPQL로 F01 엔티티 조회(결합·성능). 불변식 없는 도메인 모델 만들기(`ddd.md` 위반).
- **확인 질문:** 조회 전용 슬라이스에 도메인 모델을 두지 않고 VO 중복을 감수해도 괜찮습니까?

### D3. 검색은 어떤 잠금 읽기도 하지 않는다

- **결정:** `FOR UPDATE`·공유락·`LockModeType`·분산락 전부 안 쓴다. InnoDB 일관된 읽기 스냅샷을 그대로 쓴다.
- **이유:** 검색이 재고 행에 락을 걸면 조회 트래픽이 재고 차감을 느리게 만든다. 게다가 얻는 게 없다 — 응답 직후 재고는 또 바뀐다.
- **트레이드오프:** 이미 팔린 방을 보여줄 수 있다. **낡음을 없앨 수단을 스스로 포기**하고 그 대가를 예약 409로 받는다.
- **대안:** 공유락으로 읽는 순간만 정확하게(예약 처리량을 깎고도 응답 이후는 못 막는다). 검색이 재고를 홀드(조회가 쓰기가 된다).
- **확인 질문:** 검색 정확도를 포기하고 예약 경로에 간섭하지 않는 쪽을 택해도 괜찮습니까?

### D5. 캐시 단위를 "검색 결과 전체"로 잡고 TTL 기본 10초

- **결정:** 키 `avail:{hotelId}:{checkIn}:{checkOut}:{guestCount}:{roomCount}`로 결과를 통째로 저장한다. 재고 행 단위 캐시를 안 쓴다.
- **이유:** 히트 시 DB 쿼리가 0이라 캐시 유무 비교가 선명하다. 단순해서 캐시 on/off 동등성 증명이 쉽다. TTL은 정합성 장치가 아니라 부하 흡수 장치다.
- **트레이드오프:** **히트율을 포기한다.** 날짜 조합이 많아 키가 흩어지고, 재고 한 행이 바뀌면 그 행을 포함한 수많은 기간 조합이 낡는데 정밀히 걷어낼 방법이 없다.
- **대안:** 재고 행 단위 캐시를 MGET으로(히트율·무효화 정밀도는 낫지만 부분 미스 병합 로직이 붙어 on/off 동등성 증명이 어려워진다).
- **확인 질문:** 히트율을 포기하고 결과 캐시로 가도 괜찮습니까? (**TTL 10초라는 숫자만 바뀌는 건 설정값이라 스펙 재작성이 아닙니다. 캐시 단위를 바꾸라는 판정이면 7절을 다시 씁니다.**)

### D6. 이벤트 기반 무효화는 자리만 남기고 연결하지 않는다

- **결정:** `evict_hotel()` 진입점만 두고 F01의 재고 변경 경로와 연결하지 않는다. 기본 무효화는 TTL 만료와 `fresh=true` 재적재다.
- **이유:** (1) F01 소유 패키지에 훅을 못 넣는다. (2) 절단 1순위 캐시에 F01을 결합시키면 절단할 때 F01까지 손봐야 한다. (3) 붙여도 정합성 보장 수준이 그대로다. (4) **이벤트 단독은 오히려 위험하다** — 캐시 미스로 DB를 읽은 요청이 무효화가 지나간 뒤 자기 값을 쓰면 지울 키가 없어 무효화가 헛돌고 **낡은 값이 영구히 박힌다**(스펙 I8). TTL이 있어야 풀린다.
- **트레이드오프:** 낡음이 최대 TTL초 지속되어 **연동이 있었다면 피했을 409가 실제로 발생한다.**
- **대안:** F01 유스케이스가 커밋 후 `evict_hotel` 호출(F01 수정 승인 + 절단 결합). Redis pub/sub(같은 문제에 인프라만 는다).
- **확인 질문:** 이벤트 무효화를 자리만 남기고 이번 범위에서 빼도 괜찮습니까?

### D9. 판정은 fail-closed, 응답은 "매진 / 판매 전 / 인원 부적합"을 구분한다

- **결정:** 기간 중 하루라도 재고 행이 없으면 결과에서 제외하되, 결과가 비면 진단 쿼리 1회로 `SOLD_OUT` / `NOT_YET_OPEN` / `NO_FITTING_ROOM_TYPE`을 구분해 준다. `NOT_YET_OPEN`이면 `salesOpenUntil`(시드 기준 **2026-10-29**)도 준다.
- **이유:** 두 층의 요구가 다르다. 판정은 안전하게 틀려야 하고(행 없는 날짜를 "제약 없음"으로 보면 매진을 가용으로 표시한다), 안내는 정직해야 한다(매진이면 다른 날짜를 찾고 판매 전이면 나중에 온다 — 정반대 행동). 비용은 **결과가 빌 때만 나가는 쿼리 1회**로 정상 경로는 그대로 1회다.
- **트레이드오프:** 응답 필드 2개와 분기·테스트가 는다. 더 중요하게는 **`salesOpenUntil`이 시드에서 추론한 값**이다. 실제 서비스라면 판매 시작·종료일 컬럼이 있어야 한다.
- **대안:** 셋을 뭉쳐 빈 목록만 주기(가장 싸지만 세 경우 모두 사용자가 잘못된 행동을 한다). `room_type`에 판매 기간 컬럼 추가(F01 소유 침범, 불가).
- **확인 질문:** 빈 결과의 이유를 3종으로 구분하는 데 동의합니까? 추론값이라는 한계를 문서에 남기는 것으로 충분합니까?

### D15. 캐시 on/off 값을 하나의 프로바이더에서 내보내고 밖에서 읽을 수 있게 노출한다 (**전환으로 새로 생김**)

- **결정:** 설정 키를 `PMS_SEARCH_CACHE_ENABLED`(기본 `true`)로 두고, **캐시 구현을 고르는 쪽과 그 값을 노출하는 쪽이 같은 프로바이더를 본다.** 노출은 자체 엔드포인트가 아니라 **공용 설정 노출 계약(`app/common/runtime_report.py`의 `RuntimeContributor`)의 기여자로** 합니다 — F03 컨테이너에 기여자 하나를 등록하면 F01 소유 엔드포인트가 모아 내보냅니다.
- **이유:** 전환 전에는 "나중에 F04가 요청하면 붙인다"로 미뤄둔 항목이었습니다. **파이썬으로 오면서 조립이 틀려도 잡아줄 컴파일러가 없어졌고**, 캐시는 절단 1순위(00 D4)라 **"정말 꺼졌는가"를 확인할 수단이 필요합니다.** 새 `coding-rules.md`도 "현재 값을 외부에서 읽을 수 있는 경로를 만든다"와 "**검증 장치가 검증 대상과 다른 곳을 보면 검증이 아니다**"를 규칙으로 두었습니다.
- **왜 이게 실제 위험인가:** 껐다고 믿었는데 안 꺼져 있으면 캐시 on/off 두 회차가 같은 조건이 되고, 결과는 "차이 없음"으로 나옵니다. 그 숫자는 **"캐시가 없어도 되더라"는 정반대 결론**으로 읽힙니다. 틀린 측정보다 **틀린 줄 모르는 측정**이 나쁩니다.
- **트레이드오프:** F03 소유 범위에 기여자 구현이 하나 늘어, **"조회 전용 슬라이스"라는 성격에 운영 관심사가 조금 섞입니다.** 누적 카운터를 안 실었으므로 k6를 거치지 않는 운영자는 히트율을 즉석에서 볼 수 없습니다 — 이 프로젝트의 관찰자는 F04뿐이라 감수합니다.
- **대안:** 노출하지 않고 F04가 로그로 추정(추정은 검증이 아닙니다). F03 자체 엔드포인트(원안. 컨텍스트마다 확인 주소가 늘어 F04가 세 곳을 돌아야 합니다). 누적 히트/미스 카운터 추가(아래 판단 참조).
- **형태 (3차 갱신 — 공용 계약으로 확정):** 원안은 F03 자체 엔드포인트였는데, F02도 게이트 정보를 노출해야 해서 **컨텍스트별 기여자를 F01 엔드포인트가 모으는 방식**으로 통합됐습니다. 원안에서 "F01 소유 침범"으로 기각했던 방향인데, **`Protocol`이 `common`에 놓이면서 그 사유가 해소됐습니다** — F03은 `reservation`을 import하지 않고 `common`만 봅니다. 합쳐진 응답에서 F03의 몫은 이렇습니다.

  ```json
  { "loadTest": { "PMS_SEARCH_CACHE_ENABLED": true,
                  "PMS_SEARCH_CACHE_TTL_SECONDS": 10 },
    "implementations": { "searchCache": "RedisAvailabilityCacheAdapter" } }
  ```

  **설정 키가 대문자인 것은 규칙이 정한 예외입니다.** 다른 응답은 `camelCase`지만, 이 값만은 사람이 읽어 셸에 그대로 옮겨 적는 값이라 환경변수 이름을 그대로 씁니다. **`implementations`를 함께 내보내는 것이 핵심입니다** — 값만 보여주면 "값은 `false`인데 실제로는 Redis 구현이 물려 있는" 상태를 못 잡습니다. 클래스 이름은 손으로 적지 않고 **`type(...).__name__`으로 실물에서 뽑습니다** — 문자열 상수로 적으면 그 상수가 또 하나의 "따로 선언된 설정"이 됩니다.
- **누적 히트/미스 카운터는 싣지 않기로 판단했습니다.** F02는 카운터를 실었는데, 게이트가 자른 건수가 **응답만 봐서는 조건부 UPDATE가 자른 것과 구분되지 않기 때문**입니다. F03은 다릅니다 — **캐시의 생사가 응답마다 `source` 필드로 나가므로**, 관찰자인 k6가 히트율을 표본 단위로 직접 집계할 수 있고 실행 중 Redis가 죽는 것도 `source`가 `DB`로 쏠리는 것으로 잡힙니다. 정보가 응답에 없는 F02와 정보가 응답에 있는 F03의 차이입니다.
- **확인 질문:** 캐시 on/off 값을 공용 계약의 기여자로 노출하는 데 동의합니까? 키 이름을 `PMS_SEARCH_CACHE_ENABLED`로 두는 데 동의합니까? 실제 주입된 구현 클래스 이름까지 싣는 데 동의합니까? **누적 카운터 없이 응답의 `source` 필드로 충분하다는 판단에 동의합니까?**

---

## 4. [확인]·[참고] 10개 — 이견 있는 번호만 알려주세요

| # | 등급 | 결정 요약 | 스펙 |
|---|---|---|---|
| D1 | 확인 | F01 의존을 표 하나(A1~A10)로 모으고 병합 시 대조. 값은 다 채워졌고, 그중 A2·A8·`CHECK` 표현은 **F01 안에서만 확정**(F01 D16·D2·D3이 관리자 미확인). **셋 다 뒤집혀도 F03 재승인 불필요** | 5절 |
| D4 | 참고 | 날짜별 반복 조회 대신 집계 쿼리 한 발. 부분 낡음을 없애고 왕복을 30분의 1로 | 8절 |
| D7 | 확인 | Redis 장애 시 캐시를 미스로 강등 (fail-open). 사용자에겐 200 + `source: DB` | 7절 |
| D8 | 확인 | 인덱스를 하나도 추가하지 않는다. F01 자연키 PK가 이미 커버 | 5절 |
| D10 | 확인 | 캐시 스탬피드 방어를 F04 측정 이후로 미룬다 | 7절 |
| D11 | 참고 | 검색 API는 인증 헤더를 요구하지 않는다 | 8절 |
| D12 | 확인 | **세션 수명을 집계 쿼리 한 발로 좁히고 캐시 접근은 세션 밖에서 한다.** 전환 전 "트랜잭션을 열지 않는다"의 재표현이며 의도는 같다 | 6절 |
| D13 | 확인 | 성능용 대량 데이터(≈91,250행)를 Alembic 밖 스크립트로 만든다 | 10절 |
| D14 | 확인 | 검색과 예약의 날짜 경계가 다른 것은 층이 다르기 때문이다. 검색은 "고를 수 있는 것", 예약은 "만들 수 있는 것" | 6절 |
| **D16** | 참고 | 조회 전용이라 유스케이스 입력 타입을 `~Command`가 아니라 `~Query`로 부른다. **네이밍 규칙과 한 칸 어긋나므로 드러내 둔다** | 3절 |

이유·트레이드오프·대안은 스펙 11절에 같은 번호로 있습니다.

---

## 5. 검증 계획

쓰기가 없어 경합으로 데이터가 깨지지 않습니다. 검증할 것은 **"캐시를 껐을 때도 똑같이 동작하는가"**입니다.

| 시나리오 | 조건 | 기대 |
|---|---|---|
| C2 읽기-쓰기 경합 | 재고 1인 행을 50 스레드가 읽는 중 1이 0으로 | `minRemaining`은 1 또는 0뿐. 중간값·부분 결과 없음 |
| C4 캐시 on/off 동등성 | 20가지 조건을 두 모드로 | `items` 완전 동일 (`source`만 다름) |
| C5 Redis 다운 | 정지 후 50회 | 전부 200 + `source: DB`, 예외 0건 |
| **C6 핵심 서사** (F01 병합 후) | 재고 1, 2 스레드가 같은 캐시 결과로 예약 | 1건 201, 1건 409, **재고 음수 0건**. 재검색 시 해당 타입 소멸 |

**무효화가 실패하는 8가지(I1~I8)를 스펙 7절에 표로 정리했습니다.** I1~I4·I8은 설계상 허용된 실패이고 전부 TTL이 상한을 보장합니다. I5~I7은 구현 실수로만 생기고 **스스로 회복되지 않아** 전용 테스트로 막습니다. **어느 칸에도 초과 판매가 없습니다.**

TDD 21단계는 스펙 9절에 순서대로 있습니다. **17번(무효화를 스쳐 지나간 늦은 쓰기 재현)이 가장 값진 테스트**입니다 — 나중에 누가 "이벤트 무효화를 붙였으니 TTL은 빼도 되겠다"고 판단하면 이 테스트가 먼저 깨집니다.

---

## 6. 남은 것과 넘긴 것

**안 한 것:** 구현 코드 전부(F01 병합 후), ADR(승인 후 D3·D6를 ADR-0030 대역에), 학습 노트 등록(스펙 13절에 후보 7개만).

**넘긴 것:** F04에 `docs/spec/F03-검색-API-계약.md`(합격선 S1~S6, 시드 기준 날짜·인원 경계, **409는 오류율이 아니라 관측 지표**)와 L5 스탬피드 실측. PM에 `docs/reports/F03-00문서-갱신-제안.md`(3건).

**F01 병합 시:** 계약표 A1~A10 대조 (A10은 DDL이 아니라 `common`의 예외 코드) → `EXPLAIN`으로 인덱스 판단 확인 → C6 작성.

---

## 승인란

- [ ] 승인
- [ ] 조건부 승인 (조건: )
- [ ] 재작업 (사유: )

**표시하시면 [확인]·[참고] 항목은 일괄 동의로 처리합니다. 이견이 있는 것은 번호만 알려주세요.**

지난 회차에 [확인]·[참고] 17개가 판단 없이 남았는데, 그건 **"이걸 일일이 답해야 하나"가 분명하지 않아서** 생긴 일입니다. 위 한 줄이 그 답입니다 — **판단이 꼭 필요한 것은 3절의 [필수] 6개뿐이고, 나머지는 넘어가시면 동의로 갑니다.**
