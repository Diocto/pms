# S6 — 혼합 지속 소크 + selftest 2차 (2026-08-16 22:47~22:56)

명제 (가)(다) 종합. 초당 200건 × 5분 = 약 41,500 요청을 예약 60%·확정 20%·
취소 15%·조회 5%로 섞어 전 객실타입 × 10일(9-21~9-30)에 흘렸다. 이 시나리오의
본론은 부하가 아니라 **사후 검증**이다 — 보존식만 보면 "두 번 깎고 두 번
되돌린" 경우를 놓치므로, 예약당 복원 이력이 정확히 한 줄인지를 센다.

## 판정 — 통과 (k6·verify·selftest 모두 종료 코드 0)

| 항목 | 기대 | 실측 |
|---|---|---|
| 보존식 위반 행 (전 객실타입 × 10일) | 0행 | 0행 |
| 초과 판매(remaining<0) / 복원 과다(remaining>총량) | 0행 / 0행 | 0행 / 0행 |
| **이중 복원 (예약당 복원 이력 2줄 이상)** | 0행 | **0행** |
| 표 밖 전이 / 종료 상태 이중 도달 | 0행 | 0행 |
| 과거 체크아웃 프로브가 새어들어감 | 0 (하드 게이트) | 0 (1,198건 전부 400 거절) |
| 서버 오류 / 미지 코드 | 0 | 0 / 0 |
| p95 | <1s | 19ms (최대 1.29s) |

규모: 생성 3,719건, 취소 3,199건, 확정 787건(k6 응답 기준), 재고 부족 거절
31,018건, 락 실패 63건. 최종 상태 분포(9-21~9-30 범위): CANCELLED 3,199 ·
CONFIRMED 519 · PENDING 1(종료 직전 생성분, 이후 정상 만료 예정) — EXPIRED
0은 설계 조건("보류 10분 > 소크 5분")이 지켜졌다는 뜻이다.

k6 카운터와 이력 수치는 약 1% 어긋날 수 있다 — k6는 응답을 받은 성공만
세므로, 멱등 재확정(이력에 줄이 안 생김)이나 응답 유실 뒤 성공이 섞이면
정확히 일치하지 않는다. 그래서 판정은 k6 카운터가 아니라 이력·DB 하드
게이트로 했고, 그 게이트는 전부 0행이다.

## 과거 체크아웃 프로브 — 방어선이 한 겹뿐인 곳을 일부러 찔렀다

`checkOut > today` 규칙만은 DB CHECK로 받칠 수 없어(MySQL CHECK에 CURDATE()
불가) 애플리케이션 검증 한 겹이 전부다. 부하 중 2%(1,198건)를 지난 날짜로
섞어 넣었고 전부 400으로 막혔다. 새어든 것 0건은 하드 게이트로 걸었다.

## selftest 2차 — 이력 검증기도 검증됐다

0번(재고 쪽)에 이어 이력이 쌓인 지금 이력 쪽을 검증했다. 일부러 깨뜨린 2종 —
이력 한 줄의 event를 없는 표기로 변경, 한 예약에 복원 이벤트 두 줄 복제 —
을 검증 쿼리가 둘 다 잡았고, 복구 후 다시 조용해지는 것까지 확인했다.
재고 쪽 3종도 재확인: 보존식 깨기는 잡히고, 총량 초과·음수는 DB CHECK가
UPDATE 자체를 거부했다(3차 방어선 생존 확인).

- reset·verify·selftest 종료 코드 0, k6 종료 코드 0
- k6 원본: `s6-k6-summary.json`, 조건: 워커 4, 락 ON, 만료 기본값(10분), 재기동 후 워밍업 24건 (`settings-s6.json`)
- 판정 주체: PM 컨펌(ADR-0059)
