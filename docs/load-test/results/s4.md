# S4 — 상태 전이 경합·취소×확정 (2026-08-16 02:23)

명제 (다). 300쌍: 예약 생성 후 취소와 확정을 동시에 발사.

## 판정 — 통과 (불변식 전부. 지연 참고선 1건 미달은 아래 설명)

| 항목 | 기대 | 실측 |
|---|---|---|
| 금지 전이 통과 (거부 23칸에서 200) | **0** | **0** |
| 경합 관측 (409 `INVALID_STATE_TRANSITION`) | >0 (경합이 실제로 있었다는 증거) | 196 |
| 최종 상태가 PENDING인 쌍 | 0 | 0 |
| 서버 오류 / 404 / 미지 코드 | 0 | 0 / 0 / 0 |
| DB: 표 밖 전이 | 0행 | 0행 |
| DB: 예약당 재고 복원 2회 이상 | 0행 | 0행 |

## 실제 일어난 전이 (이력 405행 — 허용 표의 부분집합)

| 전이 | 건수 | 의미 |
|---|---|---|
| PENDING → CANCEL → CANCELLED | 194 | 취소가 선착 — 뒤따른 확정은 409 |
| PENDING → CONFIRM → CONFIRMED | 107 | 확정이 선착 |
| CONFIRMED → CANCEL → CANCELLED | 104 | 확정 후 취소 (허용 전이) |

재고 복원 이벤트 298건 = 취소로 끝난 예약 수와 일치, 복원은 예약당 정확히 1회.

## 지연 참고선 미달 — 원인 설명 (설계 실패 아님)

p95 1.14s로 참고선(<1s)을 넘었다. 600 VU가 동시에 연결을 여는 순간 연결 수립 자체가 p95 229ms(`http_req_connecting`)로, 커넥션·스레드풀 상한 대기가 지연의 몸통이다. 잰 것은 락 경합이 아니라 **한 머신에서 600 연결을 받는 비용**이다 — 리포트 §2의 한계 서술 그대로. 정확성 판정에는 영향 없음.

- reset·verify 종료 코드 0. k6는 지연 참고선 1건으로 비-0 (불변식 게이트는 전부 통과)
- k6 원본: `s4-k6-summary.json`, 조건: 워커 4, 락 ON
