# S3 — 멱등성 폭주 (2026-08-16 02:20~02:22)

명제 (나). 스탠다드(09-04), 재고 경합 없음.

## S3-A 재시도 폭주 (키 20 × 50회 = 1,000건) — 통과

| 항목 | 기대 | 실측 |
|---|---|---|
| 생성(201) | 정확히 20 | 20 |
| 같은 키에서 201 두 번 | 0 | 0 |
| 같은 키에 다른 확인번호 | 0 | 0 |
| 같은 키에 다른 금액 | 0 | 0 |
| 서버 오류 / 미지 코드 | 0 | 0 / 0 |

재반환(200) 392건, "처리 중" 거절(409 `REQUEST_IN_PROGRESS`) 593건, 락 실패(503) 15건. `REQUEST_IN_PROGRESS`도 부하에서 실물 확인됐다 — 0번에서 단건 유도가 안 됐던 마지막 코드다.

## S3-B 더블클릭 (키 100 × 2회 동시 = 200건) — 통과

| 항목 | 기대 | 실측 |
|---|---|---|
| 생성(201) | 정확히 100 | 100 |
| 중복 생성·번호/금액 불일치 | 0 | 0 |
| 서버 오류 | 0 | 0 |
| p95 | <500ms | 220ms |

## 실행에서 배운 것 — 폭주만으로는 키가 굶을 수 있다

첫 실행(마무리 재시도 없던 판)에서 20키 중 1키가 생성 0건으로 끝나 `count==20` 게이트가 회차를 잡았다. 원인: 1,000건이 0.7초에 몰리는 동안 그 키의 담당 요청이 재고 락 200ms 대기에서 번번이 503을 받았고, 같은 키의 나머지 요청은 전부 "처리 중" 409로 즉시 거절돼 재시도 몫이 소진됐다. **중복은 0건 — 정합성 문제가 아니라 가용성을 내준 것**이고, 503의 계약("잠시 뒤 다시")대로 재시도하면 성사된다.

대응: 스크립트에 **마무리 재시도 단계**를 추가했다(간격 0.2초, 키당 최대 20회). 명제 (나)의 완전한 문장으로 정정한다 — "폭주 중 몇 번 거절될 수는 있어도, 재시도가 성사되는 순간까지 합쳐 **예약은 키당 정확히 1건**이다." S3-B에서는 100키 중 다수가 이 단계에서 성사됐다(더블클릭 200건이 같은 재고 락 하나를 다퉈 폭주 단계 락 실패 64건).

- DB 검증(`verify/run.sh s3`) 종료 코드 0: 행 100 = 키 100, 키당 확인번호 1개, 재고 차감 = 행 수
- k6 원본: `s3a-k6-summary.json`·`s3b-k6-summary.json`, 조건: 워커 4, 락 ON
