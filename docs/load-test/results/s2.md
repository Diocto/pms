# S2 — 재고 경합·지속 부하 (2026-08-16 22:18)

명제 (가)의 지속 판본. 스탠다드 100실에 초당 300건 × 20초 = 6,000건을
도착률 고정으로 흘렸다. 도착률 고정인 이유: VU 고정 모델은 앱이 느려지면
요청 수도 줄어 느려진 사실이 지표에서 사라진다. 본 부하 앞에 상태를 바꾸지
않는 예열 구간(초당 50 × 30초, /health)을 두고 집계에서는 뺐다.

## 판정 — 통과 (k6·verify 종료 코드 0, 임계값 미달 없음)

| 항목 | 기대 | 실측 |
|---|---|---|
| 생성(201) | 정확히 100 | **100** |
| 재고 음수 행 | 0행 | 0행 |
| 보존식 (잔여 + 점유 = 총량) | diff 0 | 0 + 100 = 100, diff 0 |
| 최종 잔여 | 0 | 0 |
| 서버 오류 / 미지 코드 | 0 | 0 / 0 |
| 본 부하 p95 / p99 | <800ms / <2s | 205ms / 최대치 370ms |

거절 내역: 재고 부족 409가 4,460건, 락 실패 503이 1,440건 — 합 5,900 + 성공
100 = 6,000, 전량 수지 맞음. 재고가 소진된 뒤에도 부하가 계속 들어오는
시나리오라 거절이 요청의 98%인 것 자체가 정상 형태다.

폭주(S1: 200건 일괄)와 달리 도착률을 고정하면 지연이 안정된다 — 본 부하
평균 96ms, p95 205ms, 최대 370ms로 S1(p95 1.1s대)의 연결 폭주 비용이 없다.
잰 것이 락 경합 비용이라는 점에서 S5 대조의 부하 원본으로 쓰기에 적합함을
확인했다.

- reset·verify 종료 코드 0, k6 종료 코드 0
- k6 원본: `s2-k6-summary.json`, 조건: 워커 4, 락 ON, 만료 기본값, 도착률 300/s × 20s (`settings-s2.json`)
- 판정 주체: PM 컨펌(ADR-0059)
