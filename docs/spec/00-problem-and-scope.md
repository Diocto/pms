# 문제 정의와 구현 범위

- 상태: 승인됨 (D1~D6 전항 동의)
- 작성일: 2026-08-14
- 승인일: 2026-08-14

## 1. 어떤 문제를 푸는가

숙박 예약 플랫폼에서 **같은 객실을 여러 사람이 동시에 예약하려 할 때 무슨 일이 벌어지는가**를 다룬다.

예약 시스템의 진짜 어려움은 CRUD가 아니다. 다음 세 가지가 겹치는 지점에 있다.

**동시성.** 잔여 1실을 두 사람이 같은 순간에 예약하면, 순진한 구현(조회 → 판단 → 저장)은 둘 다 성공시킨다. 초과 판매다. 인기 날짜, 프로모션 오픈 순간에는 수백 요청이 같은 재고 행을 두드린다.

**멱등성.** 결제 버튼을 두 번 누르거나, 타임아웃 후 클라이언트가 재시도하면 같은 요청이 두 번 도착한다. 이걸 두 건의 예약으로 만들면 안 된다. "안 일어나겠지"가 아니라 정상 상황으로 설계해야 한다.

**상태 전이.** 예약은 생성 → 확정 → 체크인 → 체크아웃으로 흐르고, 중간에 취소·결제 실패·미결제 만료로 빠진다. 이 규칙이 if-else로 흩어져 있으면 상태 추가마다 버그가 생기고, 규칙을 문서로 증명할 수 없다.

이 셋을 **말이 아니라 수치로 증명하는 것**(동시성 테스트, k6 부하테스트)까지가 이 과제의 목표다.

### 왜 이 문제를 골랐는가 (자유 주제 선정 이유)

- 정답이 코드 스타일이 아니라 **설계 판단**에 있는 문제다. 락 선택, 트랜잭션 경계, 방어선 설계가 그대로 평가 대상이 된다.
- 검증이 객관적이다. "재고 10개에 100명이 몰렸을 때 정확히 10건만 성공"은 참·거짓이 갈리는 명제다.
- 실무 대표성이 있다. 예약·재고·결제는 어느 도메인에나 있는 구조라 면접에서 깊게 논의할 수 있다.

## 2. 서비스 서사

멀티 호텔 예약 플랫폼이다.

- 플랫폼에 **호텔**이 여러 개 있다.
- 호텔마다 **객실타입**(스탠다드, 디럭스 등)이 있고, 객실타입마다 **총 객실 수**가 있다.
- 고객은 날짜 범위와 인원으로 가용 객실을 검색하고, 객실타입 단위로 예약한다.
- 재고는 개별 객실 호수가 아니라 **객실타입 × 날짜 단위 잔여 수량**으로 관리한다. (실제 OTA들의 모델과 같다)
- 특정 날짜·객실타입에 한정 수량 **특가(프로모션)**가 열리면 트래픽이 한 점으로 몰린다.

## 3. 구현 범위

### 포함

| # | 유스케이스 | 핵심 관심사 |
|---|---|---|
| UC-1 | 가용 객실 검색 (호텔·날짜 범위·잔여 기준) | 조회 성능, Redis 캐시, 캐시-재고 정합성 |
| UC-2 | 예약 생성 | 멱등성 키, 재고 차감 원자성, 분산락, PENDING 생성 |
| UC-3 | 예약 확정 (내부 모의 결제) | 상태 전이, 결제 실패 분기, 외부 호출은 트랜잭션 밖 |
| UC-4 | 예약 취소 | 상태 전이, 재고 복원, 취소-확정 경합 |
| UC-5 | 미결제 만료 (EXPIRE) | 시간 기반 전이, 만료-확정 경합, 재고 복원 |
| UC-6 | 체크인·체크아웃 | 상태 전이 표 완성 |
| UC-7 | 선착순 특가 예약 | 한정 수량 원자적 차감, 스파이크 트래픽 |

여기에 전 구간 공통으로 **k6 부하테스트와 리포트**가 붙는다.

### 제외 (이유와 함께)

| 제외 항목 | 이유 |
|---|---|
| 호텔·객실타입 관리자 CRUD | 관리 기능은 동시성 주제와 무관하다. 호텔·객실타입·재고는 시드 데이터(마이그레이션)로 넣는다 |
| 실제 PG 연동 | 외부 의존은 모의 결제 모듈로 대체한다. 설계 포인트(트랜잭션 밖 호출, 실패 분기)는 모의로도 다 보여진다 |
| 회원가입·인증 | `X-User-Id` 헤더로 식별한다. 이 과제의 관심사가 아니다 (ADR-0006) |
| 개별 객실 호수 배정 | 체크인 시 호수 배정은 재고 문제와 다른 문제다. 객실타입 수량 모델로 충분하다 |
| 쿠폰 발급·사용 | 프로모션은 특가 재고 선착순으로 한정한다. 쿠폰은 애그리거트가 하나 더 늘어 3일 반에 부담이다 |
| 알림, 리뷰, 정산 | 주제와 무관하다 |

## 4. 도메인 개요

바운디드 컨텍스트는 둘로 본다.

```
inventory (재고)                     reservation (예약)
├── Hotel / RoomType (읽기 전용 참조)  ├── Reservation (애그리거트 루트)
├── RoomDailyInventory (애그리거트)    │     상태머신: 전이 테이블
│     불변식: remaining >= 0          │     불변식: 전이 표 밖 전이 금지
└── PromotionInventory (애그리거트)    └── (Reservation은 roomTypeId만 보유)
      불변식: remaining >= 0
```

상태 전이 표 초안. 상세는 F01 스펙에서 확정한다.

| 현재 | 이벤트 | 다음 |
|---|---|---|
| PENDING | CONFIRM (결제 성공) | CONFIRMED |
| PENDING | PAYMENT_FAILED | CANCELLED |
| PENDING | CANCEL | CANCELLED |
| PENDING | EXPIRE | EXPIRED |
| CONFIRMED | CANCEL | CANCELLED |
| CONFIRMED | CHECK_IN | CHECKED_IN |
| CHECKED_IN | CHECK_OUT | CHECKED_OUT |

표에 없는 조합은 전부 거부. CANCELLED·EXPIRED·CHECKED_OUT은 종료 상태.

## 5. feature 분할과 병렬 계획

ADR-0004(코어 순차, 확장 병렬)에 따라 나눈다.

| feature | 내용 | 소유 모듈 | 마이그레이션 대역 | 진행 |
|---|---|---|---|---|
| F01 예약 코어 | UC-2~6 전부. Reservation 애그리거트, 상태머신, 재고 차감·복원, 멱등성, 분산락 | `app/reservation/**`, `app/inventory/**` (단 `app/inventory/query/**` 제외) | `001`~`099` (공통 스키마 포함) | **단일 세션 순차** |
| F02 선착순 프로모션 | UC-7. PromotionInventory, 특가 예약 | `app/promotion/**` | `201`~`299` | F01 병합 후 병렬 |
| F03 가용 객실 검색 | UC-1. 검색 쿼리, Redis 캐시 | `app/inventory/query/**` (조회 전용) | `301`~`399` (예약만, 미사용) | F01 병합 후 병렬 |
| F04 부하테스트 | k6 시나리오·실행·리포트 | `load-test/`, `docs/load-test/` | 없음 | F02·F03과 병렬 착수 가능 |

UC-3~6이 전부 F01에 있는 이유: 모두 `Reservation` 애그리거트의 상태 전이라서, 도메인 hard 소유 원칙상 쪼개면 안 된다. 전이 표가 한 번에 완성되면 handle() 구현 자체는 작다.

### 일정 (마감 2026-08-17 23:59:59)

| 날짜 | 목표 | 상태 |
|---|---|---|
| 8/14 | 하네스 구축, 이 문서 승인, 문서 규칙(결정 검토란·다이어그램) 확정 | 완료 |
| 8/15 | F01 세션 착수: 스펙 작성 → 승인 → dev-cycle 진입. 목표는 당일 내 개발 사이클 완료 | 진행 |
| 8/16 | F01 승인·병합. F02·F03 병렬 + F04 시나리오 작성, 저녁부터 부하테스트 실행 | |
| 8/17 | 부하테스트 리포트, 제출 문서(docs/submission) 작성, 최종 점검 | |

버퍼가 없는 일정이다. **지연 시 잘라낼 순서: F03 캐시(검색은 DB 직행으로) → UC-6 체크인·체크아웃 → F02 프로모션.** F01과 부하테스트는 자르지 않는다.

## 6. 리스크

| 리스크 | 대응 |
|---|---|
| 멀티 호텔 서사로 조회·모델이 비대해짐 | 관리자 기능 전부 제외, 시드 데이터 고정. 호텔은 사실상 조회 필터일 뿐 |
| F01이 하루를 넘김 | 위 절단 순서 적용. EXPIRE 스케줄러가 밀리면 수동 API로 대체 |
| Spring Boot 4.x 레퍼런스 부족 | 이미 하네스에서 빌드·Testcontainers 검증 완료. 특이 사항은 학습 노트에 기록 |
| 부하테스트가 로컬 자원 한계에 걸림 | 리포트에 환경 한계를 명시하고 상대 비교(락 유무, 캐시 유무)로 서사 구성 |

## 7. 결정 검토란

이 문서를 쓰면서 작성자가 내린 결정입니다. 항목 번호로 동의 여부를 알려주세요. 전부 동의해야 승인입니다.

### D1. 관리자 기능 제외, 호텔·객실·재고는 시드 데이터 고정

- **결정:** 호텔·객실타입 CRUD API를 만들지 않고, 데이터는 마이그레이션으로 미리 넣는다.
- **이유:** 멀티 호텔 서사를 유지하면서 범위 폭발을 막기 위해서다. 관리 기능은 동시성 주제와 무관한 CRUD다.
- **트레이드오프:** "플랫폼"이라면서 공급자 쪽 그림이 없다. 검토관이 완결성 부족으로 볼 여지가 있다.
- **대안이었던 것:** 관리자 API 포함(액터 추가, 최소 반나절 소요), 단일 호텔로 서사 축소.
- **확인 질문:** 공급자 기능 없이 시드 데이터로 고정해도 괜찮습니까?
- **검토 결과 (2026-08-14):** 동의. 집중하는 문제가 객실 재고 관리라서 CRUD 성격의 관리자 페이지는 보류한다. 의도적 오버부킹처럼 재고 문제와 얽힌 기능이라면 만들 가치가 있지만 현재는 보류.

### D2. 확정·취소·만료·체크인아웃(UC-3~6)을 전부 F01 하나에 통합

- **결정:** 예약의 모든 상태 전이 유스케이스를 F01 한 세션이 만든다.
- **이유:** 전부 `Reservation` 애그리거트의 전이라서, 쪼개면 병렬 세션 간 의미 충돌(전이 표 불일치)이 난다. ADR-0004의 도메인 hard 소유 원칙 적용이다.
- **트레이드오프:** F01이 커져서 하루 안에 끝내야 하는 부담이 크다. F01이 밀리면 전체 일정이 민다.
- **대안이었던 것:** UC-6(체크인아웃)을 별도 feature로 분리. 전이 표 소유권이 갈라져 기각했다.
- **확인 질문:** F01 비대화를 감수하고 애그리거트 단위 통합을 유지해도 괜찮습니까?
- **검토 결과 (2026-08-14):** 동의. 각 상태에 대한 컨텍스트 유지가 중요하므로 하나의 feature로 통합해 구현한다.

### D3. 재고 모델을 "객실타입 × 날짜 잔여 수량"으로 고정

- **결정:** 개별 객실 호수는 다루지 않고, 수량으로만 재고를 관리한다.
- **이유:** 동시성 제어 대상이 명확한 행(잔여 수량)이 되고, 실제 OTA 모델과 같다.
- **트레이드오프:** "체크인 때 몇 호실?"에 답이 없다. 호수 배정은 별도 문제로 선언하고 뺐다.
- **대안이었던 것:** 객실 호수 엔티티를 두고 예약-호수 매핑. 배정 알고리즘이 추가돼 기각했다.
- **확인 질문:** 호수 배정 없는 수량 모델로 가도 괜찮습니까?
- **검토 결과 (2026-08-14):** 동의. 실제로 호수는 체크인 시 프런트에서 관리하는 방식이라 나중 문제다. 추후 오버부킹 문제를 풀 때 같이 고려한다. 특정 호실 사용 불가 같은 세밀한 관리가 필요할 수 있지만 현재 수준에서는 다루지 않는다.
- **보충 (검토 중 질문에 대한 답):** "수량만 세면 연박 손님이 방을 옮겨야 하는 배치가 나오지 않나?"라는 질문이 있었다. 답은 "연속 숙박만 있다면 나올 수 없다"이다. 날짜별 잔여가 0 밑으로 내려가지 않았다면, 체크인 날짜 순서로 그 시점의 빈 방을 배정하는 것만으로 전원 이동 없는 배정이 항상 가능하다. 내가 체크인할 때 빈 방은 나보다 늦게 체크인하는 손님의 배정에서 "사용 중"으로 보여 피해 가기 때문이다. 실무에서 방 이동이 생기는 원인은 수량 모델의 결함이 아니라 객실 고장·투숙 연장 같은 운영 이벤트이며, 이는 오버부킹 문제와 함께 다룰 영역이다. 업계 표준도 판매(타입별 수량)와 배정(체크인 무렵 호수 확정)을 분리한다.

### D4. 지연 시 절단 순서: F03 캐시 → UC-6 체크인아웃 → F02 프로모션

- **결정:** 일정이 밀리면 이 순서로 자른다. F01 코어와 F04 부하테스트는 자르지 않는다.
- **이유:** 과제의 증명 대상(동시성·멱등성·수치 증명)에서 먼 것부터 자르는 순서다.
- **트레이드오프:** 최악의 경우 프로모션 없는 제출이 되고, 스파이크 시나리오는 "예약 오픈 집중"으로 격이 낮아진다.
- **대안이었던 것:** 프로모션을 사수하고 EXPIRE를 자르는 순서. 상태머신 완결성이 깨져 기각했다.
- **확인 질문:** 이 절단 우선순위에 동의합니까?
- **검토 결과 (2026-08-14):** 동의. 프로모션은 부가 기능이므로 메인 기능을 먼저 완성한다.

### D5. 프로모션은 "특가 재고 선착순"으로 한정, 쿠폰 제외

- **결정:** 특정 날짜·객실타입에 한정 수량 특가를 여는 형태만 구현한다.
- **이유:** 기존 재고 모델을 재사용해 추가 비용이 작고, 스파이크 시나리오 소재로 충분하다.
- **트레이드오프:** 쿠폰 발급·사용이라는 더 흔한 프로모션 형태를 못 보여준다.
- **대안이었던 것:** 선착순 쿠폰(발급 애그리거트 추가 필요, 3일 반에 부담).
- **확인 질문:** 쿠폰 없이 특가 재고 방식으로 가도 괜찮습니까?
- **검토 결과 (2026-08-14):** 동의하되 조건이 있다. 쿠폰 자체는 보류하지만, 나중에 붙일 수 있도록 확장에 열린 구조(open-closed)로 자리를 남긴다. 예약 생성 입력을 주문서 형태로 잡아, 쿠폰 적용을 파라미터 추가와 도메인 계층 추가만으로 구현할 수 있게 고려만 해둔다. **F01 스펙에 이 확장 지점을 반영할 것.**

### D6. 재고와 예약을 별도 구역으로 나누고, 예약 생성만 두 구역을 한 트랜잭션으로 묶는다

- **결정:** 코드를 재고 구역과 예약 구역으로 나눈다. 각 구역은 자기 규칙만 책임진다. 재고 구역의 규칙은 "남은 방 수가 0 밑으로 내려가면 안 된다"이고, 예약 구역의 규칙은 "예약 상태는 정해진 표대로만 바뀐다"이다. 예약서는 재고 데이터를 통째로 들고 다니지 않고 객실타입 번호로만 가리킨다. 단, 예약을 만들 때는 "재고 하나 줄이기"와 "예약서 한 장 만들기"가 반드시 같이 성공하거나 같이 실패해야 하므로, 이 경우에만 두 구역을 DB의 한 작업 묶음(트랜잭션)으로 처리한다.
- **이유:** 규칙이 다른 것끼리 나눠야 코드가 섞이지 않는다. 나눠두면 병렬 작업에서 "이 구역은 이 세션 담당"이라고 경계를 그을 수 있다. 예약 생성을 한 묶음으로 처리하는 이유는, 하나만 성공하면 방은 줄었는데 예약이 없거나 예약은 있는데 방이 안 줄어든 상태가 되기 때문이다.
- **트레이드오프:** 예약 코드에서 재고 데이터를 바로 꺼내 쓰지 못하고 한 단계를 거쳐야 해서 코드가 조금 늘어난다. 그리고 우리 규칙("한 묶음에서는 한 구역만 고친다")에 예외를 두는 것이라 원칙의 일관성이 조금 깎인다. 예외를 두는 근거는 F01 스펙에 명시한다.
- **대안이었던 것:** 아예 안 나누기(경계가 없어 병렬 분담 불가), 셋으로 나누기(프로모션까지 별도 구역, F02 스펙 때 재검토).
- **확인 질문:** 재고와 예약을 구역으로 나누고, 예약 생성에서만 두 구역을 한 트랜잭션으로 묶는 예외를 허용해도 괜찮습니까?
- **검토 결과 (2026-08-14):** 동의. 재설명 과정에서 "수량 모델로 연박 손님의 방 이동 문제가 생기지 않는가"라는 질문이 나왔고, 답은 D3 보충에 기록했다.

## 8. 열린 질문

없음. (승인 시점 기준)
