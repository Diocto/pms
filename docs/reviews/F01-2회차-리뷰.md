# F01 2회차 리뷰 기록

- 일시: 2026-08-16 · 대상: 도메인 (VO·전이 표 36칸·애그리거트)
- 리뷰어 3명 병렬: `domain-reviewer`(주도) · `concurrency-reviewer` · `architecture-reviewer`
- **판정: 도메인 재작업 / 동시성 통과 / 아키텍처 통과. 심각 0 · 중요 4 · 제안 11.** 중요 4건과 제안 8건을 회차 안에서 반영했다.

## 중요 지적 4건 — 전부 반영

| 리뷰어 | 지적 | 반영 |
|---|---|---|
| domain (재작업 사유 ①) | **`now < expiresAt` 조건이 도메인 어디에도 없다** — 스펙 1.4 조건 열의 시간 조건 둘 중 체크인 창만 들어오고 확정 창이 빠졌다. 스펙 1.1의 `handle(event, now)` 부재와 함께 스펙-코드 불일치 | `assert_confirmable(now)` 추가(체크인 창과 대칭) + 경계 테스트 3개. 스펙 1.1 다이어그램을 실제 메서드 둘로 교정 |
| domain (재작업 사유 ②) | **"표가 유일한 공급원" 주장이 실제 쓰기 경로에서 거짓** — `transition()`이 임의 (expected, next) 쌍을 받아 `EXPIRED→CONFIRMED` 같은 표 밖 전이가 rowcount 1로 성공할 수 있었다 | **`apply_event(current, event)`로 재설계.** 다음 상태·복원 여부·이력 값·확정/종료 시각 전부 `resolve()`에서 파생. 이력 INSERT를 같은 메서드에 묶어 **게이트 누락이 구조적으로 불가능** (동시성 리뷰의 4회차 확인 사항도 함께 해소). 표 밖 조합·멱등 조합 테스트 추가 |
| architecture | **T26 정적 검사가 cwd에 따라 아무것도 검사하지 않고 통과** — `/tmp` 실행으로 실증됨 | subprocess grep → 절대 경로 파이썬 순회로 재작성. **모집단(파일 6개 이상) 단언** 추가. `datetime.now()`를 심어 빨개지는 것 확인 후 원복 |
| architecture | 한 글자 별칭 `_S`·`_E` (약어 금지 위반) | `_Status`·`_Event`로 개명 |

## 제안 반영 8건

- **D29 신설 — 투숙 기간 상한 30박** (동시성 리뷰: 기간 = 락 키 수. 1,200박 요청이 가용성을 공격). 구현 중 결정으로 기록, 코드+테스트 반영
- `status="PENDING"` 리터럴 → `ReservationStatus.PENDING.value` (주석-코드 일치, grep 가능)
- 가격 계산을 서비스에서 애그리거트 안으로 (자신의 데이터만 쓰는 규칙은 서비스가 아니다) — 지역 import 순환도 함께 소멸
- 테스트 예외 튜플 좁힘 — 불변식은 `InvalidRequestError`만 통과 (validator로 옮기면 빨개진다, D27 고정)
- `model_post_init` 타입 힌트, 테스트 셋업의 표 우회에 한정 주석
- **Money 자리 명시** — 두 컨텍스트 공유 VO로 inventory에 둔다는 예외를 스펙 1.2에 기록 (규칙 문서와의 긴장은 PM 판단 사안으로 이월)

## 다음 회차로 넘긴 것

- `PENDING+CONFIRM`의 `assert_confirmable` 호출이 유스케이스에 실제로 들어가는지 (4회차 T60)
- 스펙 4.1의 `ReservationPricing` — D22 해석기 기반 가격 결정으로 3회차·4회차에서 구체화. 함수 이름과 스펙 표기 정합은 그때 맞춘다

## 리뷰가 확인한 것

- 전이 표 36칸이 스펙과 한 칸도 다르지 않음 (읽는 법 주의 셋 포함), 복원 4칸 일치, 멱등의 복원 금지
- D1·D21 경계값 전부 맞는 쪽, VO 불변식 생성자 위치(D27), D7 포맷 비파싱, 혼동 문자 처리
- `occupied_dates()` 오름차순 결정성 — ISO 날짜 사전순 = 시간순이라 락 정렬과 일치
- import 방향 전수 통과, T28 유지(VO는 metadata 비오염), 시각 주입 결정성

**반영 후 전체 `pytest` 131 passed.** T26은 위반을 심어 빨개지는 것을 확인했다 (검증 도구 검증 절차).
