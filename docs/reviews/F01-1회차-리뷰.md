# F01 1회차 리뷰 기록

- 일시: 2026-08-15 · 대상: 스키마(001~052) + 조건부 UPDATE 코어 + 동시성 테스트
- 리뷰어 3명 병렬: `concurrency-reviewer` · `domain-reviewer` · `architecture-reviewer`
- **판정: 3/3 통과. 심각 0 · 중요 3 · 제안 11.** 중요 3건과 제안 6건을 회차 안에서 반영했다.

## 중요 지적 3건 — 전부 반영

| 리뷰어 | 지적 | 반영 |
|---|---|---|
| concurrency | **정렬 방어선(`sorted()`)을 잡는 경합 테스트가 없다** — 지워져도 이번 회차 전부 초록. 락을 껐을 때 InnoDB 데드락(C2)을 막는 유일한 줄인데 K8 전까지 무방비 | K8 리포지토리 판 추가 — 두 그룹이 겹치는 날짜를 **의도적으로 역순 입력**, 1213 = 0건 단언. `sorted()`를 지우고 돌려 **데드락이 실제로 나며 빨개지는 것 확인 후 원복** |
| domain | **시드 계약 검증이 id=3 한 칸뿐** — 나머지 id→속성 매핑이 무방비. capacity가 틀리면 F02·F03 정원 검증 상수가 통합 시점에 조용히 어긋난다 | 호텔 2행·객실타입 5행을 **스펙 1.9 표 그대로 튜플 전체 비교**하는 테스트 2개 추가 |
| architecture | **`app/reservation/infrastructure`에 `__init__.py`가 없어 `walk_packages` 순회에서 통째로 빠진다** — 3회차에 `lock.py`가 들어오는 순간 에러 코드 계약 검사가 그것을 못 본다 | `__init__.py` 추가 + **"app 하위 모든 패키지에 `__init__.py`가 있다" 단언 테스트** 추가로 구조적 재발 방지 |

## 제안 반영 6건

- 동시성 테스트: `barrier.wait()`를 try 안으로, future 예외 수거, **회계 닫기**(성공+부족=100, 승 1+패 49=50)
- K6 리포지토리 판 추가 — 잔여 9/총량 10에 동시 +1 복원 → 정확히 1 성공·1 감지·잔여 10
- 제약 테스트: 오류 코드 3819(CHECK)·1062(중복 키)만 통과로 좁힘 — 락 타임아웃(1205)이 통과로 새는 것 방지
- T27을 head 단정 → **조상 검사**로 (F02가 `201_`을 붙여도 안 깨진다)
- teardown을 자기 데이터로 좁힘 (이력 전건 DELETE 제거, 3개 파일)

## 다음 회차로 넘긴 지침 (제안 5건)

- **2회차:** `transition()`에 표 밖 (expected, next) 쌍이 흘러들 수 없게 전이 표를 유일한 공급원으로 묶는다. `Reservation.status`를 `ReservationStatus` 타입으로 좁힌다 (컬럼은 `String(20)` 유지)
- **4회차:** aware(KST)/naive(DATETIME) 경계 규칙 — "경계에서 naive KST로 통일"을 확정하고 만료 판정 테스트가 aware clock으로 그 경로를 밟게 한다
- **PM 전달:** `coding-rules.md`의 domain import 허용 목록에 "프레임워크 무의존인 `app/common`(예외 계층·Clock)은 예외" 명문화 제안

## 리뷰가 확인한 것 (지적 아님)

- 조건부 UPDATE 세 줄이 스펙 3.2 SQL과 의미 일치 (`synchronize_session=False`, rowcount 판정, 복원 상한)
- 세션 자가 개방 없음 (`sessionmaker`/`create_engine` import 부재 — 정적 검사 통과)
- import 방향 전수 통과, 마이그레이션 대역·체인·소유 범위 준수, `relationship()` 0건, Enum 이름=값
- 이미 적용된 마이그레이션 무수정 (origin/main과 바이트 동일)

**반영 후 전체 `pytest` 58 passed** (동시성 4종 포함).
