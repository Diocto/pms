# PM 세션 · 인수인계서

이 문서는 PM 역할을 맡는 세션의 시작점이다. 이 저장소의 문서 밖에 있는 맥락은 전제하지 않는다.

## 네 역할

- 하네스·규칙 문서(`CLAUDE.md`, `docs/architecture/**`, 템플릿, 스킬)의 유지보수와 개정
- 전체 범위 문서(`docs/spec/00-problem-and-scope.md`) 소유. feature 분할·일정 관리
- feature 세션 착수 브리핑(`docs/briefings/<feature>.md`) 작성
- 세션 간 조율: 공유 파일 변경 중재, main 병합 순서 결정
- feature 세션의 질문 응대. **답은 대화로 끝내지 않고 반드시 해당 문서에 반영한다**
- 승인 게이트에서 관리자 보조 (보고서 사전 검토, 터미널 요약)

**하지 않는 것:** 구현 코드, 테스트, feature별 스펙 작성. feature 세션이 자기 스펙부터 dev-cycle까지 전 과정을 담당한다 (CLAUDE.md 세션 역할 절).

## 프로젝트 한 줄 요약

숙박 예약 시스템 채용 과제. 마감 **2026-08-17 23:59:59 (KST)**. 본질은 동시성 제어·멱등성·상태 전이를 수치로 증명하는 것. 기능 폭보다 이 셋의 깊이가 항상 우선한다.

## 현재 상태 (2026-08-15 04:00 기준)

| 항목 | 상태 |
|---|---|
| 하네스 (규칙 5종, 스킬 6종, 리뷰어 에이전트 3종, 템플릿) | 완료, main 병합됨 |
| 빌드·인프라 (Boot 4.0.7, Gradle wrapper, docker-compose, Testcontainers 스모크 2건) | 완료, 통과 확인됨 |
| `docs/spec/00-problem-and-scope.md` | **승인됨** (결정 검토란 D1~D6 전항 동의) |
| F01 스펙 + 승인 보고서 (D1~D19) | 작성 완료. **관리자 승인 대기** (`worktree-F01`) |
| F02 스펙 + 승인 보고서 (D1~D14) | 작성 완료. 승인 대기 (`worktree-f02-promotion`) |
| F03 스펙 + API 계약 + 승인 보고서 (D1~D13) | 작성 완료. 승인 대기 (`worktree-F03`) |
| F04 시나리오(S1~S8) + k6 스크립트 골격 | 진행 중 (`worktree-F04`) |
| F02·F03·F04 브리핑 | 각 세션이 직접 작성 완료. PM 검수 대상 |
| 구현 코드 | **0건.** 스펙 승인 전이라 정상 |
| 제출 문서 (`docs/submission/`) | 구성 계획만 있음 (README) |

**병목은 승인 게이트 하나다.** 세 보고서에 D 항목이 46개 있고, 관리자 한 사람이 검토한다. 각 세션에 [필수]/[확인]/[참고] 등급 분류를 요구해뒀으니, [필수]만 항목별로 보고 나머지는 일괄 동의로 넘기는 것이 현실적이다.

## 세션 재개 절차

2026-08-15 06:00 시점에 F01·F02·F03 세션이 승인을 기다리다 종료됐다. **산출물은 전부 원격 브랜치에 있어 유실이 없다.** 각 세션이 마지막까지 커밋·푸시 규칙을 지킨 결과다.

다시 열 때는 새 세션에 아래 한 줄만 주면 된다. 대화 맥락이 필요 없다 — feature 세션이 저장소 문서만 보고 작업할 수 있어야 한다는 규칙이 여기서 값을 낸다.

| 세션 | 재개 지시 | 브랜치 |
|---|---|---|
| F01 | `docs/briefings/F01.md`를 읽고 F01 세션으로 작업을 이어라. 스펙과 구현 계획은 이미 `worktree-F01` 브랜치에 있다 | `worktree-F01` |
| F02 | `docs/briefings/F02.md`를 읽고 F02 세션으로 이어라 | `worktree-f02-promotion` |
| F03 | `docs/briefings/F03.md`를 읽고 F03 세션으로 이어라 | `worktree-F03` |
| F04 | `docs/briefings/F04.md`를 읽고 F04 세션으로 이어라 | `worktree-F04` |

재개한 세션이 가장 먼저 읽어야 할 것은 자기 브랜치의 **스펙**과 **승인 보고서**다. 거기에 결정 항목과 남은 재확인 목록이 전부 있다. F01은 `docs/spec/F01-구현-계획.md`(TDD 목록·구현 회차 계획)도 함께 읽는다.

**세션을 새로 열면 자기 worktree부터 확인한다.** 위 worktree가 아직 디스크에 있으면 그대로 쓰고, 없으면 해당 브랜치로 새로 만든다.

## 승인 후 PM이 반영할 00 문서 갱신 (대기 목록)

feature 세션은 `00-problem-and-scope.md`를 고칠 수 없다. 아래는 각 세션이 제안으로 올린 것이며, **해당 D 항목이 승인되면 PM이 한 커밋으로 처리한다.** 승인 뒤 잊히기 쉬우니 여기에 모아둔다.

| # | 갱신 대상 | 내용 | 걸린 결정 |
|---|---|---|---|
| 1 | ~~§4 상태 전이 표~~ | **불필요해짐.** 2026-08-15 관리자가 D4(`NO_SHOW` 추가)를 **기각**했다. 승인된 표를 넓힐 일이 없어졌다. 체크인 상한(`today < checkOut`)은 별개 항목으로 살아 있으나 이건 00 문서 표에 없던 것이라 갱신 대상이 아니다 | F01 D4 (기각) |
| 2 | §3 제외 표 | 문서화 후보 4개 추가 — 요금제·날짜별 가격, 다타입 묶음 예약, 세금·수수료·통화, 확정 후 날짜·타입 변경 | F01 D8 |
| 2-b | §3 제외 표 | **다섯 번째 추가** — 「노쇼 자동 처리」. 이유: 손님이 오지 않으면 운영자가 취소로 처리한다고 가정한다. 자동 전이를 두면 재고가 자동 복원되어 "노쇼 객실 재판매 안 함"(F01 D15)과 어긋난다 | F01 D4 기각의 결과 |
| 3 | §4 도메인 개요 그림 | `PromotionInventory`가 inventory 구역 안에 그려져 있음. 세 구역 분리가 승인되면 이동 | F02 D1 |
| 4 | §5 feature 분할 표 | 위와 같은 이유로 F02 소유 범위 표기 조정 | F02 D1 |
| 5 | §5 feature 분할 표 | F03 행의 Flyway 대역을 "V301~V399 (예약만, 미사용)"으로. **대역 회수는 하지 않는다** — 병합 후 EXPLAIN이 예상과 다르면 V301이 필요할 수 있다. `parallel-work.md`에는 이미 반영함 | F03 D8 |
| 6 | §3 제외 표 | 2건 추가 — 검색 결과 홀드·가예약, 전 호텔 통합 검색·정렬·페이징 | F03 제안 |
| 7 | §5 일정 표 8/16행 | "F02·F03 병렬"을 "F01 병합 후 F02·F03 구현 착수(둘 다 F01 스키마 의존)"로. 현재 문장은 두 feature가 병합을 기다린다는 사실이 드러나지 않는다 | F03 제안 |

**제안됐으나 반영하지 않기로 한 것:** 00 §6 리스크 표에 "캐시 낡음"을 넣지 않는다. 낡음은 관리해야 할 위험이 아니라 F03이 의도적으로 택한 설계이며(그 대가로 검색이 F01 쓰기 경로에 끼어들지 않는다), 리스크 표에 올리면 "해결해야 할 문제"로 읽힌다. F03이 스스로 제안하지 않기를 권했고 PM도 동의한다.

## 남은 일정 (00 문서와 동기화 유지할 것)

| 날짜 | 목표 |
|---|---|
| 8/15 | F01 세션 착수: 스펙 → 승인 → dev-cycle |
| 8/16 | F01 승인·병합. F02·F03 병렬 + F04 부하테스트 착수 |
| 8/17 | 부하테스트 리포트, 제출 문서, 최종 점검 |

지연 시 절단 순서(승인된 결정): F03 캐시 → UC-6 체크인아웃 → F02 프로모션. F01과 부하테스트는 자르지 않는다.

**진행 실적:** 8/15 04:00 시점에 스펙 4종이 전부 작성 완료돼 일정보다 앞서 있다. 원래 계획은 F02·F03을 F01 병합 후에 착수하는 것이었으나, 문서 작업은 파일이 갈려 충돌하지 않으므로 스펙까지 병렬로 당겼다. 구현은 계획대로 F01 병합 후다.

## 즉시 다음 액션

1. **F01 승인 결과를 받아 세 세션에 전파한다.** F02·F03·F04가 전부 여기에 물려 있다
2. 승인 직후 F01이 공유 파일 선행 커밋(`ClockConfig` Asia/Seoul 고정 + `application.yml`의 `pms.*` 키 5개)을 main에 올리면, 나머지 세 세션에 rebase 지시를 내린다
3. 위 「00 문서 갱신 대기 목록」을 한 커밋으로 처리한다
4. 각 세션 브리핑(`docs/briefings/F02·F03·F04.md`)을 00 문서와 대조해 검수한다. 소유 범위·Flyway 대역이 어긋나면 잡는다
5. 병합 순서 관리: feature 브랜치 → PR → 관리자 승인 후 병합. main 직접 푸시 금지

## 운영 절차 (관리자와 합의된 방식)

**승인 게이트.** 검토 문서는 `open -a Bear <파일>`로 열고 SendUserFile로도 보내며 GitHub 링크를 함께 준다. 터미널에는 3~5줄 요약. 승인 문서에는 결정 검토란(D 항목: 결정·이유·트레이드오프·대안·확인 질문)이 반드시 있고, **동의는 문서 전체가 아니라 D 항목 단위로 받는다.** 관리자는 Bear 노트에 `-> ` 접두어로 코멘트를 달기도 한다. Bear 노트는 파일로 돌아오지 않으므로 Bear DB에서 읽는다:

```bash
sqlite3 -readonly "$HOME/Library/Group Containers/9K33E3U3T4.net.shinyfrog.bear/Application Data/database.sqlite" \
  "SELECT ZTEXT FROM ZSFNOTE WHERE ZTITLE LIKE '%<문서 제목>%' ORDER BY ZMODIFICATIONDATE DESC LIMIT 1"
```

**관리자 협업 스타일.** 새 기술·개념이 나오면 결정 전에 쉬운 한국어로 설명부터 한다 (번역투·영어투 금지, CLAUDE.md 언어 절). 결정은 관리자가 내린다. 트레이드오프 없는 결정 제안은 반려된다. 결정이 나면 ADR(`decision-log` 스킬), 새 개념은 학습 노트 제목 쌓기(`learning-note` 스킬).

**git.** 계정은 Diocto (repo-local user.name/email 설정됨). 커밋 메시지 한국어. worktree에서 작업하고 PR로 병합한다. 관리자의 로컬 main checkout은 뒤처져 있을 수 있으니 병합 후 `git pull`을 안내한다.

## 주소

- 저장소: https://github.com/Diocto/pms (private, 기본 브랜치 main)
- 착수 브리핑: `docs/briefings/` (F01.md, 이 문서)
- 승인된 범위: `docs/spec/00-problem-and-scope.md`
- 의사결정 이력: `docs/decisions/INDEX.md` (ADR-0001~0006)
- 폐기된 F01 초안: git 이력 `spec-problem-and-scope` 브랜치 (복사 금지, 참고만)

## 조심할 것

- F01 세션이 `inventory.query.**`를 만들면 F03 소유권 침범이다. 브리핑에 명시돼 있지만 리뷰에서 한 번 더 본다
- 검토 반영 후보 7개를 F01 세션이 빠뜨리고 스펙을 쓰면 승인 전에 돌려보낸다 (브리핑에 지시돼 있음)
- 문서 간 불일치를 발견하면 즉시 고친다. "구현 세션은 문서만 보고 작업한다"가 이 체계의 전제라서, 문서 불일치가 곧 장애다
